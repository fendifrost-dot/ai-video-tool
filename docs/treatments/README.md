# Treatment seed artifacts

Deterministic, schema-valid treatment artifacts generated by `scripts/seed-ysl-treatment.ts`.

## YSL (Ice On) — project `764a63d2-93cd-44f3-905f-292f14ab2f51`

| File | Shape | Consumed by |
|---|---|---|
| `ysl-ice-on.treatment.json` | `StructuredTreatment` v2 (`src/lib/treatment/api.ts`) | `video_projects.treatment_json` → ShotStoryboard / Creative Director panel |
| `ysl-ice-on.shotspecs.json` | `ShotSpec[]` (`src/lib/treatment/shotSpec.ts`) | production / QA pipeline (machine contract) |

**Regenerate:** `npx tsx scripts/seed-ysl-treatment.ts` — `$0`, no network, no DB writes.

**Seeding into the real store** (Lovable-managed — no standalone Supabase):
- Preferred: open the project's Treatment Builder while authenticated; the v2 envelope drops into `treatment_json` and renders on the existing UI.
- Or Lovable SQL editor: `UPDATE video_projects SET treatment_json = <file contents> WHERE id = '764a63d2-...';` (Class B, non-destructive single-row update).

**Timing is provisional (HYPOTHESIS):** duration/BPM/sections are a director map, not audio-derived.
Re-run the generator against the analyzed `song_analyses` row to lock cut points before final edit.
