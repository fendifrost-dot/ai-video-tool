# Architecture C — ChatGPT ruling: V3 install + still-first before spend (2026-09-03)

**Status:** DECISION (ChatGPT).  
**Class:** B (prompt constants + repair UI). No paid xAI call authorized.

## Ruling

Approve the **V3 factual corrections** (collar outer mastic / navy inner only; self-colour mastic zip tape + small gold pull), but **do not spend yet**.

| Surface | Instruction |
|---------|-------------|
| V3 text | Install as preserved `GROK_VIDEO_EDIT_PROMPT_V3` with regression coverage |
| Active lane | Keep **`GROK_VIDEO_EDIT_PROMPT = V2`** / `VERSION = "v2"` |
| Spend | **SKIP** — no gated V3 run until still-first repair proof |
| Sleeve C | Deterministic `sleeve_panel` only — not a prompt experiment |

## Sequence (locked)

1. Fix the still runner (§3.4).
2. Re-run the **clean** still and prove full `chest_band` + wordmark repair.
3. Prove visible-upper-arm `sleeve_panel`.
4. **Then** authorize one gated V3 run (~$0.32) for collar/zip at generation time.
5. After that, temporal propagation.

## §3.4 runner work (this change)

- Stop auto-chaining repaired outputs as the next `logo_chest` input (block + label).
- Seed the chest quad from the measured band (≈ x 0.30–0.88, y 0.503–0.578).
- Numeric quad entry alongside drag handles.
- Warn loudly when placement looks off-garment (stage-1 face-hit class).

## Evidence links

- Defect register §4 clauses: [`ARCHITECTURE_C_V2_DEFECTS_AND_PROPOSED_FIXES_2026-09-03.md`](./ARCHITECTURE_C_V2_DEFECTS_AND_PROPOSED_FIXES_2026-09-03.md)
- Prior freeze / still-first lock: [`ARCHITECTURE_C_CHATGPT_LOCK_2026-09-03.md`](./ARCHITECTURE_C_CHATGPT_LOCK_2026-09-03.md)
