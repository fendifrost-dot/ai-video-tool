# YSL Real Video #1 · 30–60 s production validation · bars 24–46 · RESULT

**Date:** 2026-09-20 → 21 · **Author:** Claude (full takeover) · **Section:** song 47.213–90.492 s (43.28 s, bars 24–46, pre-hook + hook block) · **Spend:** see ledger below.

## What was proven

| Mandate test | Result |
|---|---|
| Song ↔ performance sync from real media | **DONE.** `song = master + 0.8538 s`, windowed GCC-PHAT, 23/30 windows consistent, drift −0.3 ppm. Record + reproducible script in PR #148. Grid 122.00 BPM measured (140 BPM seed was a hypothesis). |
| Source-range editorial model | **DONE.** `performance_syncs` + `timeline_items.source_*` (additive migration, not yet applied live) and the pure `performanceSync.ts` mapping; every shot in the treatment resolves master ranges from the song clock, none typed by hand. |
| Real editorial cutting on the song clock | **DONE.** 12 slots, cuts on bar downbeats; assembly places each render by `songToPerformance(t) − masterStart` and cuts the album WAV for the same range (`scripts/edit/assemble_section.py`, exact frame budgets per slot). |
| YSL wardrobe ON Fendi (moving footage) | **DONE — 8/8 performance slots passed Gate 0.** xAI `/v1/videos/edits` via the authenticated `grok-video-edit-proxy`, flat product photo as the single reference. Look 1 (Saint Laurent track jacket, hook, bars 32–46) prompt `v3-jacket-only`; Look 2 (YSL trucker jacket, pre-hook, bars 24–32) prompt `trucker-v1`. Identity, pose, hands, camera and background preserved; garment temporally stable. |
| ≥ 1 convincing second look | **DONE.** Two looks, both convincing on the real performance. |
| Environment transformation | **DONE (deterministic).** Person matte (U²-Net human seg + static-background prior + temporal median) over Grok Imagine plates ("cold fitting room / mirror panel"), `scripts/edit/composite_environment.py`. The v2v "change the background" pass was tested once and **rejected** (face relit into darkness, identity lost). |
| ≥ 1 purposeful B-roll cut | **DONE.** S02 real macro of the trucker jacket (fashion language on the verse); S07 night-city insert (Grok Imagine still, animated). |
| ≥ 1 transition / FX | **DONE.** S05 ice-hit white-out across the look change into the drop; S10 1/8-note mirror strobe; glitch flash into S11. |
| Return from B-roll to synced performance | **DONE** after S02, S05, S07, S10. |
| Real timeline / export | **DONE.** 1080×1920 @ 24 fps H.264 + AAC from the album master, `section_bars24-46.mp4` + `.assembly.json` (per-slot offsets). |

## Gate 0 log (moving footage)

| Shot | Look | Attempt | Asset | Cost | Decision | Note |
|---|---|---|---|---|---|---|
| S06 | 1 | 1 | `d36c0309` | $0.56 | accepted | first Gate-0 pass; row inserted manually after proxy insert failed on `shotId:"S06"` |
| S08 | 1 | 1 | `236f30f5` | $0.56 | **rejected** | chest band broken/partial through the arm-crossing |
| S08 | 1 | 2 | `2daf6190` | $0.56 | accepted | full band, stable |
| S11 | 1 | 1 | `ab3ba0f2` | $0.56 | accepted | |
| S12 | 1 | 1 | `efa48177` | $0.40 | accepted (conditional) | wordmark rendered larger than the reference |
| S09 | 1 | 1 | `8db0030f` | $0.32 | accepted | own cut (master 74.868–78.835) replaces the stale V2 clip |
| S01 | 2 | 1 | `ac4ca37d` | $0.40 | accepted | trucker: collar, silver buttons, chest flap pockets, black tee at throat |
| S03 | 2 | 1 | `a4f84701` | $0.56 | accepted | |
| S04 | 2 | 1 | `e9fb4348` | $0.32 | accepted | |
| S06 env | 1 | 1 | `255417f1` | $0.56 | **rejected** | v2v environment pass destroys identity → composite instead |

All xAI outputs are time-aligned to their source cut from frame 0 (checked by frame matching, `alignment_check.json`); xAI trims ~0.26 s from the tail, absorbed by the 0.5 s handles.

## Ledger (new spend in this takeover)

xAI video edits: 10 billed runs = **$4.80**. Grok Imagine (browser, consumer plan): plates + B-roll, $0 API. Everything else deterministic, $0. Prior handoff claim of ~$0.54 was not verifiable from the repo (no ledger file existed); per-asset `metadata_json.actual_cost_usd` is the only cost record, and every run above carries `metadata_json.provenance` (provider, model, purpose, shot, attempt, cost, decision, reason).

## Problems found → generalized fixes

1. `grok-video-edit-proxy` billed before validating `shotId` (uuid column) → output orphaned. Fix: fail-closed uuid check before any xAI call (`invalid_shot_id`). Needs redeploy.
2. V3 prompt described the on-model reference (shirt, tie, trousers) while the lane sends only the flat photo → registered `GROK_VIDEO_EDIT_PROMPT_V3_JACKET_ONLY` (jacket only, keep trousers/shoes). Same shape used for the trucker.
3. No environment lane existed that preserves identity → `composite_environment.py` (matte + plate) instead of a second generative pass.
4. No timeline → MP4 renderer existed → `assemble_section.py` (song-clock, exact frame budgets, refuses filler).
5. Grok Imagine videos are not retrievable from the sandbox (assets.grok.com 403; images are on a public CDN). B-roll video therefore came from real footage + animated stills; Chrome-download → linked Mac path needs Fendi's folder approval.

## Limitations (honest)

* 720×1280 native from xAI, upscaled to 1080×1920 — soft next to the 1080 master; not 4K.
* Wordmark legibility varies with motion (S12 oversized; fast-motion frames garble letters).
* Matte edges: hair/cap fine; occasional 1–2-px halo at cuffs on fast arm moves.
* Foreground/plate lighting direction is approximate (frontal closet light on Fendi vs left key in the plate); grade compensates, a true relight does not exist in this pipeline.
* The migration is not applied to the live DB; the treatment lives in the repo JSON, not yet in `timeline_items`.

## Scaling recommendation

The lane is production-usable per shot at ≈ $0.08/s of output with a ~1-in-8 retry rate at Gate 0. Full song (≈ 190 s performance) ≈ $16–20 in xAI edits plus retries — inside the $50 cap. Before scaling: apply the migration, write the section into `timeline_items` (source ranges + output assets), move `composite_environment.py` and `assemble_section.py` behind a worker so the UI can trigger them, and get real B-roll video out of Grok Imagine (download path or API lane).

---

## Astra visual-QA loop · v1 → v2 → v3 (2026-09-21, Claude)

The section went through the completed-draft review loop (`docs/qa/ASTRA_VISUAL_QA.md`). Astra (`gpt-6-astra`, frame strips, `watched_video:false`) reviewed the real render, not metadata.

**Review #1 (`YSL_IceOn_bars24-46_v1`, $5.19): REPAIR_REQUIRED.** 44 defects, 7 blockers — every blocker was the matte: U²-Net dropped the legs at the jacket hem on dark trousers (S08/S09 body dropouts, S06 arm erosion, S11 waist cutout) and the closet leaked beside the silhouette; plus the hook room never escalated (single mirror, no glints), the S04 1.25× crop was missing, S05/S10 were performance cross-fades instead of the specified performer-free inserts, S02's macro swung through a hand, Look 1 was wide-band/centred-white (prompt error — the flat reference has ONE narrow navy stripe with a small gold SAINT LAURENT on the wearer's left, plain shoulders), wordmark scale/placement drifted, and garment construction reset across cuts. Identity 8/10 throughout. Evidence: `astra/astra_review_v1.json`, `astra/review_v1_parts/`.

**Repair → v2 (all deterministic, $0, except wardrobe):**
* Matte: `composite_environment.py` now uses **RobustVideoMatting** (MobileNetV3 ONNX, CPU ~7 fps, auto-downloaded) — a video matter with recurrent state — instead of U²-Net/isnet; the rembg path stays as `--matte rembg`. Result: no dropouts, no closet, full body every frame (`S08_matte_rvm_v2.jpg` vs `S08_matte_u2net_v1_vs_v2rembg.jpg`). The intermediate "union of two segmenters + boundary-band background prior" rewrite is kept for the record: it fixed the dropouts but passed Fendi's shadow on the door as body.
* Hook plate: new Grok Imagine still — row of angled mirror panels, blue-white rim, glints, haze (`plates_hook_v2_candidates.jpg`, #2 chosen).
* `assemble_section.py`: per-slot punch-in from the ShotSpec (`cameraMotion.description` "1.25× crop") or `renders.json` `zoom`.
* `ysl_section_fx.py`: S05 = diamond-refraction still, push + bloom, white-out into S06's flash-in; S10 = infinity-mirror corridor still with a hard 1/8-note strobe (even eighths cold push, odd eighths mirrored negative ice flash decaying to black); S02 = locked still-life macro of the trucker's chest pocket/placket (every 2 s window of the trucker shots has a hand crossing the chest — measured on the matte); S07 unchanged (`plates_inserts_candidates.jpg`, `fx_v2_S02_S05_S10.jpg`).
* Wardrobe: Look 1 re-generated with the reference-true prompt `v3c-jacket-only-reference-true` for S06/S08/S09/S12 (`look1_v3c_wardrobe_runs.jpg`; S06 is the closest garment yet: sand-beige, narrow stripe, small gold wearer-left mark, plain shoulders). S08/S12 came back with a shirt collar + epaulettes, S09 with an outer-sleeve stripe; the collar-fix re-rolls (`v3c2`) and **S11 were refused by xAI: `permission-denied — team … has used all available credits or reached its monthly spending limit`**. S11 therefore still carries the v1 (`v3-jacket-only`) garment in v2. Spend that landed: v3b $1.52 (3 runs, still wrong lettering) + v3c $1.84 (4 runs).

**Review #2 (`YSL_IceOn_bars24-46_v2`, $5.00 + $2.3 sunk on one `max_output_tokens` incomplete part): REPAIR_REQUIRED**, scores treatment 4→6, coherence 4→6, identity 8, wardrobe 3→4, environment 4→6, edit 5→7. **20 defects resolved** (all 7 blockers, the hook escalation ×3, S04 crop ×2, S05 insert ×3, S10 insert ×4, S02 hand swing, S11 waist), 25 persisting, 4 new (edge halo minor, S06→S08 garment reset across the S07 cutaway, a 1-frame cut-label offset in the review package itself, S10 strobe "unverifiable" because the 2 fps strip sampled its white eighths). Astra's answer to question 15: "structurally a real Fendi performance enhanced by AVT — not a replacement-performer montage; visually it still advertises the AI process: garment variants, malformed branding and source leaks". Escalations to Fendi: do not accept S11's different garment as an intentional third look; a native-rate audiovisual pass of S10/the drop before sign-off (the frame strip cannot certify musical timing). `astra/astra_review_v2.json` (`_diff_vs_prev` inside), `astra/review_v2_parts/`, `astra/repair_notes_v2.txt`.

**Repair → v3 (this commit; Astra re-review deferred until the wardrobe re-rolls can run):** matte v3 — RVM's own decontaminated foreground colour across the fringe (no closet bleed), alpha remapped 0.4→0.85 (smoothstep) so the half-transparent shadow/door fringe drops out, a **static-pixel peel** in the 20-px matte band (pixels that never change across the clip — hanging closet clothes, door-edge fragments RVM attaches to the silhouette — are background; removed the white fragment beside S08's shoulder without touching the jacket), 2-px keep-dilation. Two peels were tested and rejected: a colour-match peel against the temporal-median background (bit into the sand-beige shoulder where jacket ≈ door colour) and a shadow-ratio peel (bit into the shoulder and cap); both stay in the script behind `--rvm-peel` / `--shadow-spread`, off by default. Builder: samples snapped to frame pts, cut labels use the assembler's rounded cut frame. `YSL_IceOn_bars24-46_v3.mp4` assembled from `renders_v3.json` (`section_v3.assembly.json`, `section_v3_contact_sheet.jpg`).

**Still open, by owner:** `wardrobe_generation` + `temporal_propagation` — S11 re-roll, collar fixes for S08/S12, sleeve fix for S09 (xAI credit; ~$0.56 per 7 s shot) — construction will keep varying shot-to-shot until the lane is conditioned on one hero frame; `brand_repair` — no deterministic wordmark repair for moving footage exists yet (v3c lettering is gold, wearer-left, but malformed/oversized); `environment` — Astra reads the pre-hook single mirror as a dark slab (aesthetic alternatives exist → Fendi); `sync` — one-frame lip sync and musical landing are not certifiable from silent frame strips (Fendi's native-rate check). OpenAI spend on Astra so far ≈ $16 of the $30 credit (two full reviews, one incomplete part, one 504-sunk attempt).

**Full-outfit lane (added 2026-09-21 after Fendi: "the outfit swap is only placing the jacket on me and not the entire YSL outfit — it should be in AVT").** The lane replaced only the jacket by design (flat photo only, prompt keeps trousers/shoes) after the V3 prompt that described shirt/tie/trousers without their reference broke on moving footage. The outfits are composed Looks in AVT (`artist_looks.wardrobe_feature_ids`). `grok-video-edit-proxy` now accepts `lookId` → one flat reference per piece (+ on-model for the first piece under `referenceMode:"full_look"`, ≤ 4 refs) and records `look_id` / `reference_mode` on the asset; `GROK_VIDEO_EDIT_PROMPT_V4_FULL_LOOK` describes the campaign look (jacket open, striped shirt, striped tie, black pleated trousers). Not run yet: xAI limit + Fendi's choice of Looks. Expect every full-outfit run to be a fresh sample (≈ $0.56 per 7 s shot; 8 slots ≈ $4–5) and expect the trousers/shirt to add their own construction drift; the matte and the environment pipeline are unaffected (they key on the person, not the garment).

## Full-outfit pass → v5 (2026-09-21, after Fendi's xAI top-up)

**Lane:** `grok-video-edit-proxy` with `lookId` + `referenceMode: "full_look"` (redeployed by Fendi). Looks: pre-hook = *Look B — Noir Hook 2* (`7e7760e6`: trucker + Mick Long Jeans + glasses), hook = *White Ice — Full Outfit (jacket + SL trousers)* (`ce7f6350`, created for this lane). Glasses kept, not the campaign shades (Fendi).

**Pass 1 (`v4-full-look-*`, 8 runs, $3.68):** 8/8 slots landed the whole outfit with identity intact (`look1_v4_full_outfit_pass1.jpg`, `look2_v4_full_outfit_pass1.jpg`). Gate-0 fail: S04's trucker came back short-sleeved → re-rolled with a long-sleeve clause ($0.32, passed). Hook jackets came back OPEN over the shirt/tie with fold-down collars — the on-model photo shows the jacket that way. **Fendi's correction (with three ysl.com crops):** stripe on the INSIDE of the sleeves, STAND collar (navy inside, mastic outside), jacket ZIPPED like the reference. The crops were attached to the jacket feature in AVT as `detail` references (`track_detail_collar_zip.jpg`, `track_detail_sleeve_inside_flat.jpg`); the proxy's hero-piece reference count had to change in code for them to reach xAI (one redeploy) — and that is the structural bug Fendi called out: **reference policy must be data**. Fixed in this PR: `referencePolicy` on the request / `reference_policy` in the Look recipe, code keeps only the provider cap.

**Pass 2 (`v4b`, 5 hook runs, $2.40):** S06 matched the reference in one roll (zipped, stand collar, shirt + tie at the neck, gold mark wearer-left); S08/S09/S11/S12 still came back open. **Pass 3 (`v4c`, 4 runs, $1.84):** the construction facts moved to the FIRST sentence of the prompt ("THE JACKET IS ZIPPED CLOSED. Its collar STANDS UP. Its sleeves are PLAIN on the outside.") — 4/4 landed (`look1_v4b_details_pass.jpg`, `look1_v4c_zipped_first_pass.jpg`). Lesson for the prompt registry: construction constraints go first, garment poetry after. Remaining drift: S08's sleeves render lighter than its body; S01's trucker is a lighter grey wash than S03/S04.

**v5** = pass-1 pre-hook (S01/S03/S04b) + v4b S06 + v4c S08/S09/S11/S12, matte v3, B-roll from the new generic `render_broll_slots.py` (`broll_recipes.json`), same plates/assembly (`section_v5.assembly.json`, `section_v5_contact_sheet.jpg`). xAI this round: $8.24. Astra review #3 on v5 with the v2 defect list: see `astra/astra_review_v5.json`.

**Review #3 (`YSL_IceOn_bars24-46_v5`, $5.28 + ≈$2.3 sunk on one incomplete transitions part): REPAIR_REQUIRED**, scores treatment 6→5, coherence 6→5, identity 8, wardrobe 4→3, environment 6→5, edit 7→6. Astra marked **nothing resolved** from v2: all 29 defects re-reported as "PRESENT, changed manifestation" (the outfit is on him, but every shot is still a different approximation — cuffs, collar depth, hem, sleeve tone, lettering shapes), plus four new: `S08-HEM-CONSTRUCTION-DRIFT` (waistband seam appears/disappears within the shot), `SEQ-MATTE-FOREGROUND-EROSION` (S11/S12 34.6–41.8 s; Claude's frame check `section_v5_S11_S12_matte_check.jpg` shows a pale shoulder halo at 36.5–37.0 s and a blurred hand at 41.5 s, no face loss at 1080p — disputed at native resolution, listed for Fendi), and two verification gates (S05 flash peak, S11 glitch) that a 2–12 fps strip cannot certify. Escalations: full-motion authenticity approval by Fendi after garment/matte correction; musical timing only from the audible native export.

**Where the loop stands (honest):** the deterministic subsystems have converged — matte dropouts and closet leaks are gone (blockers 7 → 0), the hook room, inserts, crop and macro are on treatment. What Astra keeps failing is **product truth on moving footage**: each xAI edit is an independent sample, so construction and lettering vary shot to shot no matter how the prompt is ordered (v4 → v4b → v4c improved the hit rate from 1/5 to 4/4 on the zip/collar/sleeve facts, but cuffs, hem and typography still drift). That ceiling is the generator's, not the pipeline's. The lever that changes it is the one the still-repair lane already argues for: **condition every shot on one approved hero frame per Look** (still-first, then propagate), plus a deterministic wordmark repair for moving footage. Matte: the remaining halo is the performer's shadow on the door, which every segmenter reads as body; a shadow-aware refinement (trimap + colour model, or RVM at a higher downsample ratio) is the next matte task. Spend: xAI $16.40 total this takeover, OpenAI ≈ $24 of the $30 credit.

## Ruling round (2026-09-21, after Astra #3) — mechanisms changed at $0

ChatGPT's ruling (`docs/VIDEO_LANE_CHATGPT_YSL_V5_RULING_2026-09-21.md`): stop buying re-rolls and reviews; change the mechanism. What changed, with evidence in this folder:

| Ruling | Mechanism | Evidence |
|---|---|---|
| §4 deterministic moving-brand repair | `scripts/edit/garment_graphic_track.py` on S08 v4c: the band segment with the wordmark cropped from the product photo (`track_flat`), anchored on frame 30, tracked with the affine model, colour-matched once to the anchor plane, shaded by the footage, occluded by the crossing arm. QA: confidence mean 0.93 / min 0.39, corner jitter 1.3 px mean / 2.7 px p95, 0 frames lost, 0 frames dropped, occlusion mean 5 %. | `S08_wordmark_track_qa_sheet.jpg`, `S08_wordmark_track.json`; side-by-side clip sent to Fendi in chat |
| §5 shadow-aware matte | `composite_environment.py --refine shadow` on S11/S12/S08 (`env6/`): the door halo along the shoulders goes, cap/face/glasses edges unchanged (no erosion). | `section_v5_S11_matte_shadow_aware_edges.jpg` (source · v5 matte · shadow-aware, four edge crops at 2×); `section_v5_S08_matte_shadow_aware_edges.jpg` — on S08 the waist-side leak Astra called `HOOK-SOURCE-LEAK` is essentially unchanged: it sits inside RVM's confident core, which the refinement never touches by design, so it is a segmentation error for the propagation/hero lane to remove, not a band problem |
| §6 B-roll deterministic | `render_broll_slots.py --plan` wrote the four recipes into the ShotSpecs; render from the recorded recipes is decoded-md5-identical to the v5 B-roll. | `docs/treatments/ysl-ice-on.section-bars24-46.shotspecs.json` (`generation.parameters.broll`) |
| §3 Look truth hierarchy | Both Looks carry `hero_feature_id`, `reference_policy`, `constraints`, `spec` as data; the proxy composes constraints first and the spec last; provider ceiling in `_shared/providerCapabilities.ts`. | proxy dry run after Fendi's redeploy will show `referencePlan` / `promptComposition` |
| §2 canonical Look | Contract verified documentarily: no first-frame conditioning on `/videos/edits`. Propagation lane designed for $0 execution on the v5 edits. | `docs/CANONICAL_LOOK_PROPAGATION_DESIGN_2026-09-21.md` |
| §7 Astra boundary | Builder states what silent sampled frames cannot certify; aggregator lists `_native_media_qa_required`. | `scripts/qa/build_astra_review_package.py` (`UNOBSERVABLE`, `EVIDENCE_BOUNDARY`) |

Known limit of the brand layer today: the generator drew the S08 band longer than the true garment, so a stub of its lettering survives to the right of the patch in a few frames — the layer covers the plane anchored on the reference geometry and reports the rest; the fix for that stub is the garment (canonical-Look propagation), not a bigger patch. No new xAI or OpenAI spend this round. Astra #4 held.

## Propagation test (2026-09-21, $0) — S06, one hero → real footage

`scripts/edit/propagate_keyframe.py`. One hero (S06 edit f72) carried across the master by chained dense flow: within ±8 frames the garment is one realisation (78 % of the garment carried within ±10 frames, occluded 4 %), flicker 10 % lower than the xAI edit, generator drift measured at 19/255 near the hero and 36/255 across the shot. Reach at ≥ 80 % coverage: 6 frames back, 3 forward → re-anchor cadence ≈ 12–16 frames on this dance footage; DIS chained, DIS direct and RAFT-small direct all fall off at the same rate, so the motion is the limiter, not the flow engine. Seven heroes at a 24-frame cadence cover 64 % with flicker 25 % lower than the edit but visible switches (they are seven different samples). Full numbers: `S06_propagation_single_hero_qa.json`, `S06_propagation_7_heroes_qa.json`; frames: `S06_propagation_qa_sheet.jpg`; design and cost reasoning: `docs/CANONICAL_LOOK_PROPAGATION_DESIGN_2026-09-21.md` §5. Verdict against the locked kill criterion: viable at a 12–16-frame cadence with heroes that share one realisation (E2), not viable with one hero per shot.

Also this round: the redeployed `grok-video-edit-proxy` verified by dry run — reads the Looks' `constraints` (composed first), `spec` (composed last), `reference_policy`, `hero_feature_id`; reference plan = on-model, flat, two detail crops (4 refs). One data/code mismatch found and fixed: a `detail` crop whose label contained "worn" was promoted to on-model by the label heuristic — an explicit `angle` is now authoritative in both copies of the reference sorter.

## Canonical-Look round → v6 (2026-09-21, ≈ $6.3 of the $50 testing budget)

**E2 (hero stills, $1.68).** 14 scene frames of S06 → `grok-image-garment-proxy` with the approved anchor (S11 v4c f80) as `<IMAGE_1>` + one flat product reference. Provider limit found and pinned: `xai:images/edits` accepts at most 3 input images (5 → HTTP 400, unbilled). The stills share the anchor's realisation (`S06_hero_stills_qa.json`, `S06_hero_stills_sheet.jpg`: anchor similarity flat across stills) but the generator moves arms/hands relative to the scene frame, so a still cannot be pasted back as a pose-locked hero; `propagate_keyframe.py --hero-stills` (silhouette intersection + head exclusion) removes the ghosts but the moved-arm frames remain unusable. E2 is kept as tooling, not as the production path.

**E1 (anchor as the Look's outfit sheet, ≈ $4.6).** Setting the approved Look-on-artist frame as `outfit_sheet_path` on the Look row — data only, the deployed proxy already sends the outfit sheet first with the "approved realisation" sentence — makes `/videos/edits` reproduce that realisation across shots. All 8 performance slots re-rolled: hook S06/S08/S09/S11/S12 on the S11 anchor, pre-hook S01/S03/S04 on the S04 v4b f60 anchor (`look_e1_anchored_S01_S03_S09.jpg`). One retry (S09: first anchored roll grew a hood and a vertical placket stripe; `v4c-anchor-e1b` adds one negative sentence for the crossed-arm pose). Residual deviation: S09's folded forearms show a sleeve stripe on the visible side.

**Brand layer + matte + assembly.** `garment_graphic_track.py` gained multi-anchor ownership, a quad shape gate (collapsed fits lose the frame) and the island stray-mark eraser (the generator's extra "SAINT LAURENT" glyphs above the band are inpainted; the tie, hands and the real stripe are untouched because they touch the band edge). Applied to S06 (2 anchors), S08, S09, S11, S12 (3 anchors + eraser; `S12_wordmark_track_qa_sheet.jpg`, `S09_wordmark_track_qa_sheet.jpg`). All eight slots composited with `--refine shadow`; B-roll rendered from the recorded plan against the v6 renders (`renders_v6.json`); `section_v6.assembly.json`, `section_v6_vs_v5_contact_sheet.jpg`. v6 = 43.279 s, 1080×1920 @ 24, album audio, sent to Fendi as a 720p preview.

**What v6 changes for the loop.** The garment is now one realisation per Look across cuts (the defect class Astra #3 flagged as generator-bound). Not yet re-reviewed: Astra #4 needs Fendi's OpenAI top-up (≈ $5–7 for a targeted shot review, inside the $50).

## Verify → repair → QA round (2026-09-22): v7, v8, targeted Astra

**Live config verified ($0).** The deployed `grok-image-garment-proxy` records `provider_capability.maxReferenceImages = 5` on its child Look rows — the pre-fix constant; `grok-video-edit-proxy` is unaffected (its key's default is unchanged). Two fixes, both Fendi's: set the `PROVIDER_CAPABILITIES_JSON` secret to `{"xai:images/edits":{"maxReferenceImages":3}}` (read at request time, no redeploy) or redeploy that ONE function after PR #157 merges. E2 is not on the production path, so v6–v8 did not depend on it. Second provider limit found: `/videos/edits` rejects prompts longer than 4096 characters (unbilled 400) — now `maxPromptChars` in `providerCapabilities.ts` and a fail-closed `prompt_too_long` response in the video proxy that reports how much of the composed length is Look data (needs the proxy redeploy to take effect).

**S09 repair (E1, $1.28 for four rolls incl. two unbilled 400s).** Root cause of the outer-sleeve stripe: the deployed reference sorter promoted the "stripe inside the sleeve, worn" detail crop to a primary reference (label heuristic; fixed on main since PR #155 but not deployed). $0 data fix: the crop's label no longer contains a promotion word, so the dry-run plan is outfit sheet → on-model → flat → collar detail → sleeve-flat detail. Rolls: e1c #1 — plain upper arms but fronts open (rejected); e1c #2 — plain upper arms, zip closed, stand collar with the tie knot at the neck (kept; `S09_repair_v6_vs_v7.jpg`). v7 = v6 with S09 replaced.

**Native-media QA (`scripts/qa/native_media_qa.py`, new, $0)** on v6/v7/v8: timeline and audio exact (43.279 s); garment flow-compensated residual 2.7–4.1 per shot (threshold 12); anchor similarity per Look — hook spread 0.185 (v6) → 0.103 (v7, after S09) with all shots ≥ 0.58; matte silhouette IoU judged as a ratio to the same measure on the master footage: hook composites ≥ 1.0 (no worse than real footage), pre-hook 0.88–0.95 (S04 flagged at 0.88). `native_media_qa_v7.json`, `native_media_qa_v7_sheet.jpg`.

**Targeted Astra review of v7 ($1.09, one `mechanisms` part, 27 frames incl. S09 before/after; `astra/review_v7_targeted_parts/`, `astra_review_v7_targeted.json`).** Verdicts: wordmark IMPROVED (0.92), matte IMPROVED (0.88), canonical_look UNCHANGED, temporal_garment UNCHANGED, construction REGRESSED, S09_repair REGRESSED; `ready_for_full_review: false`. Reading the cited frames: the "open zipper / open collar" findings on S06/S09/S11/S12 are the approved realisation itself — zip closed with the shirt collar and tie knot visible inside the standing collar — which the brief did not state, so Astra scored the anchor's own construction as a defect (brief error, corrected for the next pass). Two findings were real and NEW: **S12-DUPLICATE-WORDMARK** (stray lettering survived at frames the eraser missed because the tracked plane drifted between anchors, so the mark's blob touched the erase band's edge and was exempted) and **S11-CHEST-BAND-INCOMPLETE** (the generator omitted the wearer-right half of the chest band).

**Repairs → v8 ($0).** Tracker: nearest-anchor ownership (`--proximity-weight`), a morphological open that breaks thin bridges between a stray mark and the tie/zip before the island test, a fourth S12 anchor; result: no stray lettering in any S12 frame (checked every frame). S11: the missing band half is a deterministic tracked plain band (`gfx/band_wearer_right.png`, the product photo's band segment) placed by three measured anchors, tracked with `--track-expand 2.5` (the graphic sits on a textureless panel, so the tracker uses the surrounding zip/tie/band texture) and without anchor colour matching. v8 = v7 with S11 and S12 replaced; native QA unchanged (hook spread 0.097). Second targeted Astra pass on v8 with the garment-truth paragraph in the brief: see the next entry.

**Targeted Astra review of v8 ($0.76; `astra/review_v8_targeted_parts/`, `astra_review_v8_targeted.json`).** With the garment-truth paragraph in the brief: S12_duplicate_lettering IMPROVED (resolved at every sample); S09_repair accepted (the "open collar" regression WITHDRAWN under the clarified truth — the v7 S09 roll stands); S11_band REGRESSED (the first band track broke into pieces at one pose and crossed the tie — fixed after the review with quads ending at the zip, six measured anchors and a stricter occluder threshold → v9); construction / canonical_look UNCHANGED with narrowed scope: what remains is real cross-shot construction variance that the anchor did not eliminate — S08 split lower fronts with shirt tails (LOOK1-CONSTRUCTION-MISMATCH), collar facing/height variants S06 vs S08 vs S11, cuff exposure, tie-pattern changes across S09/S11/S12, the pre-hook S01 vs S03 denim texture/hardware difference. `ready_for_full_review: false`. Astra's stated criterion for readiness is one garment realisation per Look across shots; the anchor mechanism got the closure, collar type, band and wordmark to agree but not collar facing, hem/waistband, cuff exposure or the tie pattern.

**Where this leaves the loop.** Objective, deterministic repairs are done (S09 roll, S11 band, S12 lettering); the remaining defects are generator-side and shot-specific (S08 is the worst). Options for the next mechanism change, in order of cost: (a) per-shot best-of-N anchored rolls scored by a deterministic construction check against the anchor (N=2–3 → $1.1–1.7 per shot; S08 first), (b) Architecture C propagation of construction landmarks from the anchor (still the fallback the ruling named), (c) accept and move on to the treatment questions. A full-sequence Astra review is not warranted yet by Astra's own readiness call.

## Provider mechanism smoke on S08 (2026-09-23): Runway Aleph 2.0 vs the existing xAI E1 roll

Path exercised end to end: browser → AVT `runway-video-edit-proxy` → Control Center `video-providers-runway-video-edit` (credential stays in Control Center) → Runway `POST /v1/video_to_video`; status through Control Center's `video-providers-job-status`. Three AVT defects found and fixed on the way (all on main): the plan's signed-URL redaction ran over serialised JSON and threw on every valid request (bare 503); a synchronous poll was cut by the gateway at ~150 s while Runway kept rendering, so accepted jobs are now recorded in `provider_jobs` before any wait and settled through the proxy's `finalizeProviderJobRowId` mode; edit-mode requests must not carry `contentModeration` or `ratio` (two unbilled 400s; Runway: "output orientation follows the input video and resolution is 720p"). The last of these also needs the Control Center contract change (`ratio` injected from its model table) before Gemini Omni Flash 1.1 can run — delivered as a patch, not yet on Control Center main, so **Omni is still pending**.

**Aleph 2.0 on S08** (task `985fe022…`, Control Center job `75e07092…`, ≈ $1.96; keyframe = frame 84 of the anchored E1 roll with the shirt tail deterministically repaired, at 3.5 s; 609-char instruction). Output 612×1088 @ 30 fps, 6.97 s, no audio track. Evidence in `smoke_s08/aleph/`:

| Check | Existing E1 S08 | Aleph 2.0 S08 |
|---|---|---|
| Mechanism | xAI `/videos/edits` + undocumented reference images | Runway `video_to_video` edited to ONE keyframe |
| Cost | $0.56 / roll | $1.96 / roll (28 credits/s, 7 s) |
| Edit vs regenerate (`edit_fidelity.py`) | EDIT — outside-garment change 6.7, head 5.1, pose median 2.5 px / p90 21.7, motion corr 0.98 | EDIT — 7.5, 5.3, 2.9 px / 22.7, motion corr 0.91 (ratio 0.91: slightly smoothed motion) |
| Construction (`construction_score.py`, v7 scorer) | median 0.742 (pass; refs 0.67–0.76) | median 0.702 (above the 0.649 floor; every zone within tolerance; chest intrusion at the ceiling) |
| Construction (Astra, targeted, $0.46) | shirt tails below the hem, larger wordmark | IMPROVED: hem closed, no shirt tails, one small mark — but slanted pocket openings, outer-sleeve seam lines and dark zip edging the anchor lacks (major), a short rounded collar instead of the tall stand (major), shallower band (minor) |
| Identity | face pixels untouched by the edit (local) | whole frame re-rendered: beard/jaw and mouth locally reshaped at 6.0 s (Astra, major); head-region flow vs master median ≤ 1.3 px, p90 within E1's range — the man is the same, the pixels are not his |
| Source motion / pose | preserved | preserved to the same numbers as E1; hand/fist rendering differs at 6.0 s (Astra calls it pose drift; the flow says ≤ 1.3 px) |
| Temporal stability | motion-compensated garment residual 21.3 (master 22.0) | 19.1 — at least as stable as the footage; Astra: consistent construction across samples |
| Occlusion | arms cross the band correctly | same; band and collar survive the crossed arms |
| Branding | generator lettering, replaced by the deterministic layer | Runway drew its own small script mark; same layer applies, band cleaner |
| Resolution | 720×1280 | 612×1088 (below E1; Runway edit mode is "720p" nominal) |
| Major failure | shirt tails at the hem (best-of-N target) | canonical details invented (pockets, seams, collar shape); face re-rendered; lower resolution |
| Verdict | keeps its place as the production mechanism for S08 | not promoted: fixes E1's hem defect but adds construction deviations of its own, re-renders the face and loses resolution |

**Mechanism ruling (S08, evidence above): A — continue E1 with best-of-N selected by the scorer.** Aleph 2.0's real strength is what Architecture C also offers (one correct frame propagated), and it did carry the repaired hem through the shot; but it does not reproduce the keyframe's construction faithfully (it re-designs details), and it re-renders the performer's face at lower resolution, which is a worse trade for a real-performance video than a generator-side hem defect that best-of-N (or the deterministic hem repair) can remove. Gemini Omni Flash 1.1 ($0.70) is still worth its one run once the Control Center contract patch lands — it is the cheapest of the three and the only one whose conditioning is image references like E1's. Seedance stays held. Spend this round: Runway ≈ $1.96, OpenAI ≈ $0.46.

Scorer note: the Runway output exposed three weaknesses in `construction_score.py` that are now fixed on main (stripe landmark limited to a vertical band and to runs spanning ≥ 25 % of the garment width — a full-frame re-render put the cap into the changed-region mask and its dark visor won the landmark; hysteresis garment mask; zone histograms over the anchor's colour classes only). One limit stays: the hem zone cannot flag shirt tails because the anchor's hem legitimately contains the shirt class — shirt tails are an Astra/visual call.

### Gemini Omni Flash 1.1 on S08 (2026-09-23/24): two provider-side timeouts, $0

Claude Code landed the Control Center contract fix (`fendi-control-center` PR #18, `feb676c`, deployed): edit mode sends neither `ratio` nor `contentModeration`. Dry run through the full path then passed — HTTP 200, Runway body `model, videoUri, mode, promptText, duration, references`, the same 3115-char composed prompt and 4 references as before, estimate $0.70. The real run was submitted twice (23:27 and 00:07 UTC; tasks `787b1b26…`, `c0c292bf…`). Both reached progress 0.98 within about two minutes and stayed there until Runway failed them at ~40 minutes with `THIRD_PARTY.TIMEOUT` ("Timed out"); both charged 0 credits. Runway's own estimate for each task was 81 credits (12 credits/s for 6.71 s, not the pricing page's 10) — the AVT model entry now estimates at 12 credits/s so the cost gate stays conservative. The AVT lane behaved as designed on both: bounded wait → `provider_jobs` row → finalize loop → failure recorded on the row (the second loop ended early on an expired app-session JWT, by design any error ends the loop; a finalize call after the token refresh settled it). Evidence: `smoke_s08/omni/provenance.json`.

**What this changes:** nothing in the ruling. Omni Flash edit mode cannot be evaluated for this input today — two identical provider-side timeouts on a 6.7 s clip with four references is a provider fault, not a lane fault, and it was free. **Ruling A stands** (E1 + scorer-selected best-of-N for S08). If Omni Flash is retried later, do it with the same runner after checking Runway's status page, and note that a shorter clip or fewer references is the only variable worth changing. Seedance 2.5 remains held. Spend this sub-round: $0.

## Back to the section (2026-09-24): ledger, S08 best-of-2, deterministic hem repair, v10 → v11, first project-level Astra review since v5

**Ledger first (`docs/LEDGER_YSL_TESTING_BUDGET.md`).** The handoff header had been reporting cumulative takeover spend across two funding sources; only spend after the $50 budget was set (rev 24) counts. Reconciled: $12.91 of $50 used after this round (Aleph corrected to its billed 168 credits = $1.68; S09 repair corrected to the two billed rolls = $0.64). Separate constraint: the $30 OpenAI credit has ≈ $2.8 left after the two reviews below — a three-level Astra review no longer fits; the whole-sequence part sampled at 1 s does ($0.88).

**S08 E1 best-of-2 ($1.12, kill criterion applied).** Two more anchored E1 rolls of S08. Scorer: E1 0.742 (passes), R2 0.730, R1 0.716 — neither beats the accepted roll; both new rolls also hang the jacket fronts open with the tie exposed. With 3/3 E1 rolls of this shot reproducing the source's untucked shirt as a tail below the hem, the defect is inherited from the source performance, not sampled — no further rolls. Evidence `smoke_s08/e1_bestof2/`.

**Deterministic hem repair (`scripts/edit/hem_repair.py`, $0).** Anchor-learned classes; the intruder is described by features (light, colour-neutral, finely textured — a pinstriped shirt), not by an anchor class (the anchor's shirt is only a collar and k-means merges it with the jacket highlight); the hem region is laid out from the stripe landmark with the clip-median stripe height (per-frame heights jitter 24–40 px) and the nearest landmark is borrowed on the 27 frames where the crossing arm hides the band, so the repair never flickers off; per column, the hem is found by climbing to a smooth jacket window and the trousers by walking down to a horizontally averaged dark row (a navy pinstripe is vertically continuous and would otherwise pass for trousers); the hem line is median + Gaussian smoothed across columns and median-smoothed over 5 frames; tails are filled with the trousers' local colour and grain, the gap between the two fronts with jacket from above, and pale remnants are inpainted. QA on S08: intruder fraction in the hem region 0.029 → 0.0005 (median), 161/161 frames repaired, no zero-fill frame; native-media residual p95 5.6 (v8: 5.9). Limit found on S11: cuffs next to hands are a different geometry — the tool paints black bars there; do not apply it to cuffs.

**v10 → project-level Astra ($0.88, `astra_v10/sequence.json`): REPAIR_REQUIRED**, scores treatment 6 · coherence 5 · identity 9 · wardrobe 4 · environment 8 · edit 7. Astra answers, verbatim in substance: identity holds throughout (0.9); the closet no longer reads and the hook room escalates the cold-room concept (0.95); inserts return to the performer in the intended slots; the concept survives but "the finish does not" — it reads as a Fendi-led music-video edit that is visibly AI-dressed rather than release-ready luxury photography. Defects: two NEW and bounded — `S08-HEM-REPAIR-MASK-DAMAGE` (stair-stepped hem, pale remnants) and `S06-SHOULDER-MATTE-CUTOUT` (upper arm cut away at 00:20.5–00:21.6) — and the generator-side product-truth set carried from v8 (Look 2 denim material/hardware and cut-to-cut drift on S01/S03/S04 + S02 macro; Look 1 body length/hem band/pockets varying across S06/S08/S09/S11/S12; S08 lower fronts; S11 band integration and occlusion). Two accepted styling exceptions are noted, not blocking.

**Bounded repairs → v11 ($0).** (1) S08 hem: continuous Gaussian hem boundary, split-front gap filled with jacket, remnant inpaint. (2) S06 re-composited with the RVM alpha floor 0.40 → 0.25 and a 3-frame temporal window (the fast arm at the end of the shot was falling below the floor). Everything else preserved. `out/YSL_IceOn_bars24-46_v11.mp4` (43.279 s, 1080×1920, album-master audio).

**v11 re-review ($0.91, `astra_v11/`): REPAIR_REQUIRED**, scores treatment 6 · coherence 6 · identity 8 · wardrobe 4 · environment 9 · edit 7. Astra holds both bounded defects PRESENT at its checkpoints (S06@00:20.5 arm, S08@00:26.5 hem notches/residue). Checked at native resolution (`astra_v10/v11_checkpoints_*.jpg`): the S06 finding is real — the fast wearer-right arm is dropped by the matter for ~0.5 s at the end of the shot; the S08 finding is small at native res (a few-pixel notch and a pale sliver at the zip) but real.

**v12 ($0).** S08 hem line smoothed as one edge across all repaired columns (`--hem-smooth 51`, 7-frame temporal median): the notches are gone at the 8 sampled instants (`s08_hem_repair/hem3_consistency_8_instants.jpg`); the one residual is the white tail beside the crossing hand for a few frames (the climb-up is blocked by skin). S06: a temporal-MAX matte (`composite_environment.py --temporal-mode max`) restores the arm but lets the closet leak back around head and shoulders (`s06_matte_median_vs_temporalmax.jpg`) — rejected; v12 keeps the median matte, so `S06-SHOULDER-MATTE-CUTOUT` stays open (needs a motion-aware matte: a second matting pass at a higher RVM downsample ratio, or a flow-warped alpha from the neighbouring frames, both $0 and not yet built). Native-media QA v12: timeline/audio exact, all wardrobe slots OK (S04 silhouette borderline, unchanged since v8), S08 residual p95 5.6.

**Release-intent grade for v12 (evidence: Astra v10/v11 sequence reviews, native QA v8–v12, construction scorer):** the section is a coherent, performer-led, treatment-conformant edit (identity 8–9, environment 8–9, edit 7) whose finish fails on product truth — wardrobe 4. Every remaining blocker is generator-side and shot-to-shot: Look 2's denim material and hardware (S01/S03/S04 + S02 macro) and Look 1's body/hem-band/pocket variance across S06/S08/S09/S11/S12, plus S11's band integration. None of those is reachable by the deterministic repairs that exist; they need either propagation of one approved realisation across shots (Architecture C on the E1 edits) or a provider that edits to a keyframe faithfully (Aleph re-designed details; Omni timed out). Grade: **not release-intent; strongest achievable under the current mechanisms, with the S06 arm dropout the one open composite defect.**

## Architecture C garment-body propagation on S08 (2026-09-24, $0): checkpoint B

**Directive (ChatGPT via Fendi):** preserve ONE approved garment realisation through motion without letting the generator redesign it; first test on one representative failing shot, deterministic QA over the whole shot, contact sheet AND native-rate comparison, success = materially better product truth than the accepted E1 shot without damage to identity, motion, hands/occlusion, temporal stability or edit compatibility; no Astra credit during development; no paid generation.

**Setup.** Shot S08 (161 frames @ 24, 720×1280 working size; master `S08_master_69.466-76.368`, accepted edit = the E1 anchored roll with the deterministic hem repair, `gfx/s08_e1_graphic_hem3.mp4`). Heroes are selected by construction truth (`propagate_keyframe.py --anchor/--anchor-master`): every edit frame is scored by `construction_score.py` against the approved Look-on-artist frame (S11 f80), the canonical hero is the best frame (f86, 0.789), re-anchors every ≤ 16 frames prefer frames that score well and agree with the canonical zone map → heroes `[1, 13, 29, 44, 60, 71, 86, 95, 108, 118, 136, 146, 160]`. Everything else is the existing Architecture C worker (DIS flow chains with accumulated forward–backward drift → confidence, occluder residual on the master, RVM silhouette, garment mask = what the edit changed, edit frame as fallback, per-frame QA). Twelve runs; evidence in `archc_s08/` (`qa_S08_c*.json` per run, `qa_S11_c12.json` for the S11 data point, `construction_scores_E1_vs_c8.json`, `S08_archc_contact_sheet.jpg`, `S08_archc_chest_zoom_E1_c8_c10.jpg`, `qa_sheet_c8/c10.jpg`, defect crops); the native-rate `compare.mp4` (master | E1 | propagated) of runs 8 and 10 were sent to Fendi in chat.

**What was wrong with the worker for garment bodies and what was fixed ($0, all in `scripts/edit/propagate_keyframe.py`, mechanism-level, no S08 constants):**

| Run | Change | Measured effect (S08, whole shot) |
|---|---|---|
| 1 | overlap mode: each pixel takes the hero chain with the higher confidence | coverage 0.67; visible **mottling** even at hero frames (f146: 0.658 vs E1 0.742) — a patchwork of 2–3 heroes whose E1 realisations differ in shading (`run1_overlap_mottling_master_E1_c1.jpg`) |
| 2 | nearest-hero ownership, conf 0.75 | mottling gone; **ownership pops**: flow-compensated temporal residual 16–24 at every midpoint+1 frame (E1 itself 5–10 there) |
| 3–4 | `--blend`: both neighbouring heroes carried, the far warp registered onto the near one by a smooth field fitted on textured pixels (chain drift ≈ 5 px per chain at a midpoint is low-frequency), cross-fade by temporal distance | pops gone (p95 10.1 vs E1 10.3); with a 6-px registration cap the wordmark ghosted (the two chains disagree by ≈ 9 px), fixed at 16 px + σ12 field |
| 5–6 | relight from the **edit's own** garment shading (mask-normalised σ21 luminance ratio) instead of the camo master's; per-hero relight before mixing; ownership weights smoothed σ6 | camo print-through blotches gone; coverage 0.82, fallback 0.014, temporal 4.82 / p95 10.1 vs E1 5.93 / 9.86 |
| 7–8 | both warps glide to the in-between geometry (A by w, B by 1−w) with the shift **composed into the chain maps** (one cubic resample) | p95 8.6 vs E1 9.9 with no midpoint spikes; sharpness ratio (Laplacian energy in the painted garment vs E1) 0.85 mean / 0.65 p10 (0.50 when resampled twice) |
| 9 | anatomy: garment-colour gate on the edit frame (Lab radius 20 around the Look's k=6 colours), hero masks restricted to the RVM silhouette, occluder threshold 20 | a hallucinated door patch no longer painted (f14); hands still smeared where they move fast (f42) |
| 10 | motion gate in the chain (px/frame above 10 counts as drift) + fallback to the edit where the two registered warps disagree (> 25) | hands clean, cuff ghost (f126) mostly gone — but coverage 0.71, fallback 0.08 and the fallback toggling makes temporal residual **worse** than E1 (5.70 / 12.6 vs 5.31 / 9.8) |
| 11–12 | `--harmonise`: the canonical hero's texture carried hop by hop to every re-anchor (maps composed, one resample, registered, photometrically verified against the hero's own garment, relit) | canonical coverage per hero **0.52 → 0.48 → 0.22 → 0.14 → 0.05**, and **0.00 across the arm-crossing (heroes 60 → 44)**; same on S11 (0.55 / 0.36 / 0.30 / 0.16 → 0.04). Where it did arrive, the texture changed the hero by only **3.6 RGB units mean** |

**Deterministic verdict against the success gate (best non-harmonised config, run 8, vs the accepted E1 shot):**

| Check | E1 (accepted) | Arch C run 8 | Reading |
|---|---|---|---|
| construction score median / p10 / std (`construction_score.py --step 2`) | 0.705 / 0.647 / 0.040 | 0.708 / 0.636 / 0.042 | no material change; per-zone stds identical within 0.01 (hem 0.109 vs 0.115, lower front 0.107 vs 0.119) — **the within-shot variance the scorer sees is pose and lighting, not construction**: with 83 % of the garment painted from frames ≤ 8 frames away, construction is fixed by design and the numbers do not move |
| flow-compensated temporal residual mean / p95 | 5.93 / 9.92 | 4.92 / 8.61 | 17 % smoother; no midpoint pops |
| sharpness ratio (painted garment vs E1) | 1.00 | 0.85 mean, 0.65 p10 | one resample plus a two-hero blend softens the knit; visible on the wordmark at midpoints in the zoom sheet |
| coverage / fallback / occluded | — | 0.83 / 0.014 / 0.16 | |
| hands / occlusion law | real hands | hero garment painted over motion-blurred hands at f42, f150 (run 8); clean in run 10 at the cost of the temporal number | the flow that carries the mask hole with the hand fails exactly where the hand moves fastest |
| identity, camera, timing, edit compatibility | — | untouched (only the garment mask is painted; same frames, same length) | |

**Ruling: B — strong evidence the current design cannot solve garment-body propagation as a product-truth fix, with the exact limitation named.** Three findings, each measured:

1. **On S08 the accepted E1 shot has no within-shot construction drift left for propagation to remove.** The scorer and its zone stds do not move (table above); the harmonisation carry changed hero textures by 3.6 RGB units where it landed (i.e. neighbouring E1 heroes already share the realisation to within relight noise); at contact-sheet scale the twelve runs are indistinguishable from E1 (`S08_archc_contact_sheet.jpg`). S08's product-truth defects — collar facing vs S06/S11, hem band and pockets vs the anchor, the lower fronts — are properties of the E1 realisation itself and of other shots, not frame-to-frame redesign. Astra's within-shot finding (`S08-HEM-CONSTRUCTION-DRIFT`, v5) belonged to the v4c roll and was already gone in the E1 roll + hem repair.
2. **Dense-flow chains cannot transport one texture across a 7 s performance.** Hero-to-hero carry with re-registration and photometric verification reaches 0.5 of the garment after one hop and ≈ 0 after three on both S08 and S11, and 0 through the arm-crossing segment; the chain's own confidence over a whole 16-frame hop covers 0.3–0.7 of the garment at conf 0.6. So "ONE realisation across the shot" degenerates to "thirteen E1 realisations, each carried ± 8 frames" — which is what E1 already is.
3. **The cross-shot problem is out of reach of pixel propagation by construction.** The truth hierarchy puts the approved canonical realisation (S11 f80) above the E1 motion, but a flow chain can only carry pixels of the same footage; importing the S11 realisation into S08's pose needs a pose-locked re-render of the approved realisation at S08 hero frames (E2 stills were not pose-locked — rev 24 — and Aleph re-designed details — rev 28), after which this propagator's `--hero-stills` path applies.

What the round did produce, kept on main because it is mechanism: hero selection by construction truth; the `--blend` two-hero carry with registered, glided, single-resample geometry (no ownership pops); edit-shading relight; the anatomy gates; the motion gate and disagreement fallback; per-frame sharpness and temporal residual in the QA; `--harmonise` with its coverage report (the instrument that measured finding 2). None of it is S08-specific. Cost this round: $0; ledger unchanged at $13.82; Astra untouched (≈ $1.90 OpenAI credit left).

**Recommendation for the next scaling decision (Fendi/ChatGPT):** stop treating propagation as the product-truth fix on the E1 edits — it can only smooth what E1 already keeps. The remaining wardrobe-4 blockers need a better **hero**, not a better carrier: a pose-locked realisation of the approved Look at one frame per shot (paid: an image edit that keeps the master frame's pose and takes the anchor as the garment — the E2 contract with a pose lock, or a keyframe-faithful provider if one appears), which this propagator can then carry ± 8 frames per hero with the numbers above; or accept the E1 realisations and move to the treatment questions. Until one of those is chosen, scaling to the full song reproduces the wardrobe-4 class.

## Pose-locked hero contract (2026-09-25, $0): checkpoint A — gate calibrated, scheduler measured, provider experiment designed

Directive after checkpoint B: the carrier is not the primary problem, the hero is; build the deterministic hero QA first, calibrate it on existing assets, design the scheduler from measured carrier reach, expose hero generation behind a provider-neutral interface, then stop before the first paid call. Full record: `docs/POSE_LOCKED_HERO_CONTRACT_2026-09-25.md`; evidence `hero_gate_calibration/`.

**Gate (`scripts/qa/hero_gate.py`).** Six independent checks — pose fidelity (MediaPipe landmarks in torso units + arm angles + head), identity (face SSIM/Lab), silhouette (RVM IoU on head + hands only), construction (`construction_score.py`'s anchor model, now exposed as `build_anchor_model` / `score_with_model`), material (body/stripe ΔE, texture, stripe proportion), anatomy (hands keep the source hand's chroma, landmarks present, one silhouette) — pose lock = pose AND face, fail-closed. Calibrated on 14 candidates with `--expect`, all agreeing: source frame pose PASS; four accepted E1 frames pose PASS (strict mean 0.02–0.04); five old E2 stills pose FAIL (mean 0.05–0.47, arm angles to 122°, faces re-rendered SSIM 0.31–0.48 — the stills `hero_stills_qa.py` had accepted 14/14); wrong frame and wrong shot FAIL; the approved anchor PASSes end to end; Aleph 2.0's f86 frame PASSes (its invented seams at other frames are outside what a colour-class scorer sees — recorded as the gate's structural blind spot, left to the brand layer and Astra).

**Scheduler (`scripts/edit/hero_schedule.py`).** Trial heroes every 6 frames, the propagator's own chain measures reach, confident masks are unioned the way the two-hero blend carries, greedy cover. S08: at ≥ 0.8 coverage with the motion gate **22 heroes** and 24 frames (fast hands) never reachable; at ≥ 0.7 without the gate 15 heroes, 2 unreachable; single-hero reach −2.7/+3.1 frames at the strict standard, −5.1/+5.8 at the loose one. "±8" (rev 31) was the mean-coverage figure; per-frame ≥ 0.8 needs a hero every 6–8 frames on this motion — the scaling number.

**Interface (`supabase/functions/_shared/poseLockedHero.ts`, 5 deno tests).** `buildHeroRequest()` routes an abstract request (source frame, Look, canonical realisation, product/identity refs, constraints) to the existing edge function for the chosen mechanism; providers ranked by geometry guarantee (mask > try-on > prompt > none) then cost; the pose-lock law is composed first in every prompt. Registry rows carry dated evidence for `fal_inpaint_masked` (jacket-inpaint-proxy), `fal_vton` (wardrobe-vton-proxy), `xai_image_edit` (grok-image-garment-proxy), `runway_image_reference` (unverified; docs describe reference composition, not editing).

**First paid experiment (designed, not run):** S08 f84 (scheduled hero, best-scoring pose of the shot for the E1 realisation), `xai_image_edit` ×3 with the law composed first and the anchor as `<IMAGE_1>` (≈ $0.36) and, allowlist permitting, `fal_inpaint_masked` ×2 (≈ $0.10); every candidate gated; a PASS goes into `propagate_keyframe.py --hero-stills` for the ±window test (checkpoint B); no PASS in five → stop those mechanisms. ≈ $0.50, ledger row on result. Spend this round: $0 (ledger unchanged at $13.82; OpenAI ≈ $1.90 reserved).

## First paid pose-locked hero experiment — S08 f84 (2026-09-25): checkpoint B, 0 of 5 pass, both mechanisms stopped

Authorised by ChatGPT via Fendi within the contract's stop conditions (§6 of `docs/POSE_LOCKED_HERO_CONTRACT_2026-09-25.md`): one source frame (S08 f84 = 3.5 s, master frame at 1080×1920, the scheduled hero whose E1 realisation scores best), `xai_image_edit` ×3, `fal_inpaint_masked` ×2 if the CC allowlist carried `fal-ai/flux-lora/inpainting` (it does, `switchx-restyle/index.ts` line 658), every candidate through `hero_gate.py` at commit `6c5d396` with the calibrated thresholds, no manual override. Evidence and full provenance (hashes of source, anchor, product reference and every output; client prompt and its hash; server composition; models; costs; raw gate output): `hero_exp_s08_f84/`.

| candidate | provider / model | pose lock | pose (strict max / mean / arm° / head) | identity SSIM | silhouette IoU | construction | material | anatomy | verdict |
|---|---|---|---|---|---|---|---|---|---|
| xai_c0 `f0e372d9` | xAI `/v1/images/edits`, grok-imagine-image-quality (no override set), 3 images | **FAIL** | 0.43 / 0.141 / 33° / 0.074 | 0.39 | 0.72 | 0.565 | FAIL (texture 3.2, stripe 2.3×) | hands unseen | FAIL |
| xai_c1 `efe7c9be` | same | **FAIL** | 0.47 / 0.134 / 39° / 0.073 | 0.38 | 0.71 | 0.558 | FAIL | hands unseen | FAIL |
| xai_c2 `b62f5bab` | same | **FAIL** | 0.38 / 0.145 / 25° / 0.104 | 0.33 | 0.68 | 0.553 | FAIL | hands unseen | FAIL |
| fal_m0 `49cdf3ae` (seed 777) | fal `flux-lora/inpainting` via jacket-inpaint-proxy (evf-sam mask, face guard, 30 steps, strength 0.85) | **FAIL** | 0.46 / 0.081 / 47° / 0.013 | 1.00 | 1.00 | 0.315 | pass | hand_l 0.9 | FAIL |
| fal_m1 `a559d245` (seed 777) | same — **byte-identical to m0** (deterministic default seed) | FAIL | same | 1.00 | 1.00 | 0.315 | pass | — | FAIL |
| fal_m2 `d4c36db5` (seed 778) | same, second distinct candidate | **FAIL** | 0.45 / 0.084 / 65° / 0.013 | 1.00 | 1.00 | 0.376 | FAIL (body ΔE 7.4 → pass; stripe/texture fail) | hands 0.99 / 1.05 | FAIL |
| E1 f84 (accepted roll, control) | xAI `/videos/edits` E1 | PASS | 0.03 / 0.014 / 3° / 0.018 | 0.89 | 0.98 | 0.787 | pass | 1.08 / 1.04 | **PASS** |

**What each mechanism did (contact sheet `hero_exp_sheet.jpg`, skeletons: green = source, red = candidate).** The three xAI stills are the E2 failure exactly, with the pose-lock law composed into the prompt: the performer is re-posed into a symmetrical portrait (arms folded, shoulders squared, head frontal, face redrawn — SSIM 0.33–0.39), the wordmark becomes a multi-line block of pseudo-text, the knit is rendered ribbed (texture ratio 2.5–3.2); the garment itself is the right colours (body ΔE 2–3, stripe ΔE 3–5). The masked lane did the opposite: identity and silhouette are perfect (SSIM 1.00, IoU 1.00 — the mask holds geometry by construction), but the inpainter, which has no conditioning on the approved realisation, left the camo shirt in place (construction 0.32–0.38, lower-front and hem zones 0.0) and painted a navy-and-white track sleeve across the crossed forearm and over part of a hand (`fal_m2_hands_crop.jpg`) — the pose-lock FAIL on this lane is the landmarker reacting to that repaint (wrist/index displaced 0.45, arm angle 47–65°), i.e. the anatomy law broken inside the mask, not a moved body.

**Costs.** xAI actual $0.24 (`cost_in_usd_ticks` 800000000 × 3); fal not readable from AVT, carried at the proxy's estimate $0.36 for three runs — the third run was spent because the first two shared the default seed and returned one image; tranche carried $0.60 against the ≈ $0.50 cap (xAI came in $0.12 under its estimate). Ledger rows 11–12: **$14.42 used / $35.58 remaining**.

**Ruling per the stop rule: both mechanisms stop.** Exact failures by candidate are in the table; in one sentence each: xAI image edit fails the pose-lock law by construction of the mechanism (a whole-image editor obeys the pose instruction no better with the law up front than without it — 0/3 here, 0/14 in E2); flux-lora masked inpainting fails the garment-truth law by construction of the lane (no conditioning on the approved realisation, so it cannot reproduce it) and breaks the anatomy law inside its own mask. No candidate reached Architecture C; the ±window measurement was not run.

**Newly discovered blind spots / gate behaviour.** (1) On a masked candidate the gate reports a repainted forearm/hand as a POSE failure (landmarks move on repainted anatomy) — correct verdict, imprecise attribution; the anatomy check only looks at hand-back discs and missed the sleeve painted over the forearm. (2) The masked lane's outputs are deterministic at the default seed: "N candidates" from that lane must vary the seed or they are one candidate. (3) The xAI stills copy the anchor's frontal composition more than the source's — the anchor as `<IMAGE_1>` may itself be pulling the pose; a source-only control (no anchor) was not in this contract and remains untested.

**What would change the answer (not run, not authorised).** A mechanism that conditions on the approved realisation AND holds geometry: masked inpainting with the anchor as an appearance reference (flux-general + IP-Adapter is on the CC allowlist and is the Guarded-Grok path that timed out at ~946 s in August; it needs the anchor, not a Grok render, as the IP-Adapter image), or an instruction-editing model with a reference slot that is documented to preserve the input photo. Both are registry rows, not architecture changes.
