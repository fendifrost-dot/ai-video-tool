# CLAUDE → repo · latest handoff

> **Convention.** This file is always Claude's most recent handoff. Claude overwrites it each time; dated copies live in `docs/` and `docs/research/results/`. Cursor and ChatGPT: "check the repo" means read this file. Cursor's side is `docs/handoffs/CURSOR_LATEST.md`.

**Updated:** 2026-09-23 (rev 28 — **S08 provider smoke run; ruling A**: Runway lane live end to end behind Control Center (three AVT defects fixed on main: JSON-corrupting redaction, gateway-cut synchronous poll → `provider_jobs` + `finalizeProviderJobRowId`, edit-mode `ratio`/`contentModeration`); Aleph 2.0 on S08 = EDIT, hem fixed, but invented pockets/seams/collar, face re-rendered, 612×1088 → not promoted; Omni Flash pending one Control Center contract patch; scorer hardened for full-frame re-renders · rev 27 — Runway executes behind Control Center · rev 26 — provider capability checkpoint + deterministic construction scorer) · **Author:** Claude (full takeover, executive mode per Fendi 2026-09-23) · **Spend this takeover:** ≈ $23.4 xAI + ≈ $26.4 OpenAI (+ $0.46 targeted A/B) + **≈ $1.96 Runway** (Aleph 2.0 S08, through Control Center) · **No paid call pending** (Omni Flash ≈ $0.70 waits for the Control Center patch)

## State in one paragraph

**Latest (rev 28 · S08 provider smoke, 2026-09-23, ≈ $2.4):** The Runway lane works end to end (AVT `runway-video-edit-proxy` → Control Center `video-providers-runway-video-edit` → Runway; status via Control Center job-status; nothing provider-side in AVT). Three AVT defects found live and fixed on main: (1) `redact(JSON.stringify(...))` corrupted the plan JSON (`token=[^&]+` ran past the closing quote) — every valid request died with a bare 503; now value-level `redactDeep`. (2) The gateway cut the synchronous poll at ~150 s while Runway rendered on — accepted jobs are now inserted into `provider_jobs` before any wait, the proxy waits ≤ 100 s, and `{ finalizeProviderJobRowId }` settles the job later (idempotent; the smoke runner loops it). (3) Edit-mode requests may not carry `contentModeration` or `ratio` (Runway: orientation follows the input, 720p) — AVT fixed; **Control Center still injects `ratio` from its model table** → patch delivered (`contract.ts` + test, 18 tests pass) but it could not be committed from this session; until it lands, `gemini_omni_flash_1.1` returns an unbilled 400. **Aleph 2.0 on S08** (task `985fe022…`, $1.96): fidelity EDIT with E1-level pose numbers; scorer median 0.702 (floor 0.649); Astra targeted A/B ($0.46): construction IMPROVED (hem closed, no shirt tails) but pockets/seam lines/short collar invented, face locally re-rendered, 612×1088 output. **Ruling: A** — keep E1 + scorer-selected best-of-N for S08; run Omni Flash once for $0.70 when Control Center is patched; Seedance held; Architecture C unchanged. Details and table: results doc "Provider mechanism smoke on S08". Scorer hardened (`construction_score.py`: landmark band + width gate, hysteresis mask, class-only zone histograms); known limit: shirt tails are not a scorer call. Frontend: `vite build` and Lovable's own type check pass on main; the "Build unsuccessful" badge on every GitHub push is not a repo defect (no CI exists; Lovable's sandbox builds clean) — most likely Lovable-side, possibly the account's payment-failure banner; vitest: 1364 pass, 2 pre-existing 5 s timeouts in `videoQaFullClip.test.ts`.

**Latest (rev 27 · Runway lane behind Control Center, 2026-09-23, $0):** The provider boundary is closed and the 503 is fixed. **Control Center** gained `video-providers-runway-video-edit` (CC PR #17, `cfeb26e`, deployed): Runway `POST /v1/video_to_video` for `aleph2` / `gemini_omni_flash_1.1` / `seedance2_5`, its own canonical credit table, break-glass `RUNWAY_VIDEO_EDIT_DISABLED`, audit through `tool_execution_logs`, and a spend gate that refuses before calling when CC's own `ccProviderEstimateCents` exceeds AVT's `avtAuthorizedMaxCents`. The generate / job-status / job-result functions and `_shared/video-providers/proxy.ts` are untouched. **AVT** (PR #161, `8fd57f2`) dropped the direct-Runway slice — no `RUNWAY_API_KEY`, no `api.dev.runwayml.com`, no task polling — and kept what is AVT's: ownership, Look truth hierarchy, reference/keyframe signing, cost authorization, dry run, persistence, provenance. **The 503 (rev 27 fix):** every valid body returned a bare 503 with **no CORS headers** while every guard answered correctly in JSON. Perfect correlation — each failing path was the only one that reached `callControlCenter`, and PR #161 had routed it through AVT's own `proxy-provider-call`, i.e. a synchronous **same-project edge→edge invocation** during a request. That is the gateway answering for a killed worker, which no `try/catch` in the caller can intercept. Nothing else in the repo does it: `ingest-provider-job` and `proxy-provider-call` both call the CC **project** directly, and the other same-project `/functions/v1/` URLs are callback addresses handed to remote services. Fixed by `_shared/controlCenterClient.ts` — a direct cross-project call with `x-api-key`, one implementation, `avt_user_id` stamped from the JWT this function verifies itself. Also: the whole handler is wrapped so an unhandled throw returns `{error:"unhandled_exception", detail:<stack>}` with CORS instead of a bare 503; `control_center_not_configured` fails closed on a billed call while the **dry run still returns the local plan**; and the four `deno check` errors are gone (`SupabaseClient<any,any,any>`, `Required<ReferencePolicy>`) so the deploy pipeline cannot reject the file. **Needs Fendi:** redeploy AVT `runway-video-edit-proxy` only (CC is already deployed; `proxy-provider-call` is unchanged by the fix). Then the browser session re-runs the two $0 dry runs. Tests 1366 passed / 1 skipped; `deno check` clean; no paid call made.

**Earlier (rev 26 · capability checkpoint, 2026-09-23, $0):** PR #158 merged (`391b60f`). ChatGPT paused the paid S08 rolls for a provider check; result in `docs/PROVIDER_CAPABILITY_CHECK_2026-09-23.md`. **xAI:** the 5-image editing limit is `grok-imagine-image-2.0`; our `grok-imagine-image-quality` is the 3-image model and is retired 2026-11-02 → `providerCapabilities.ts` is now keyed `provider:operation[:model]` with both models recorded, both proxies pass their model, the image proxy's default model is a secret (`XAI_IMAGE_EDIT_MODEL`). The official video-editing page documents **no reference images** for `/videos/edits` (input ≤ 8.7 s, 720p) — E1's anchor conditioning is an undocumented parameter that happens to work; reference-to-video (7 refs, try-on) is generation only → out. Grok 4.7 = one secret (`GROK_DIRECTOR_MODEL`) whenever wanted. **Runway:** `video_to_video` hosts `aleph2` (edit this video guided by ≤ 5 keyframes = edited frames of the same video, ≤ 30 s, $0.28/s), `gemini_omni_flash_1.1` (mode=edit + 5 image refs, ≤ 10 s, up to 4K, $0.10/s) and `seedance2_5` (mode=edit + 30 refs, $0.45/s at 720p). Aleph 2.0 is Architecture C's premise run by the provider — it attacks the construction-drift failure if a correct S08 keyframe exists (the best-scoring frame of the current S08 roll is pose-exact). **Written ($0):** `supabase/functions/runway-video-edit-proxy` (same contract as the xAI lane: Look truth hierarchy, cost gate, provenance; models as data; fails closed without `RUNWAY_API_KEY`), Runway capability rows, `scripts/qa/construction_score.py` — the deterministic selection rule for best-of-N and for the A/B: anchor-learned colour classes, stripe landmark, garment-relative zones (neck/chest/lower-front/hem/sleeves), zone similarity + foreign-class intrusion, pass = within the accepted shots' band; calibration (`construction_score_calibration.json`) rejects S09 open-front roll, S08 v4c broken band, S09 outer-arm-stripe roll and flags S08 e1's shirt tails at the hem — the four known defects. **Smoke authorised by ChatGPT with a test-order change (2026-09-23):** Aleph 2.0 and Gemini Omni Flash 1.1 FIRST (≈ $2.7), no fresh xAI control unless the existing S08 E1 evidence is unfair, Seedance held, Astra only if deterministic QA is ambiguous. Prepared at $0: pose-correct S08 keyframe (frame 84 of the anchored roll, shirt tail deterministically repaired, uploaded), `runway_smoke_runner.browser.js`, `edit_fidelity.py` (EDIT vs REGENERATED; baseline: E1/v4c EDIT, wrong shot REGENERATED). **Blocked on Fendi:** deploy Control Center `video-providers-runway-video-edit`, then redeploy AVT `proxy-provider-call` + `runway-video-edit-proxy`. (Superseded 2026-09-23: the Runway credential stays in Control Center; no AVT secret is added.)

**Latest (rev 25 · verify → repair → targeted QA, 2026-09-22):** **(1) Live config.** The deployed `grok-image-garment-proxy` still runs with `xai:images/edits = 5` (its child Look rows record `provider_capability.maxReferenceImages: 5`); the video proxy's key is unchanged. Fix = Fendi: set the `PROVIDER_CAPABILITIES_JSON` secret `{"xai:images/edits":{"maxReferenceImages":3}}` (no redeploy) or redeploy that one function now that PR #157 is on main. Not blocking (E2 is tooling, not the production path). New provider limit: `/videos/edits` prompt ≤ 4096 chars (unbilled 400) → `maxPromptChars` capability + a fail-closed `prompt_too_long` in the video proxy (needs redeploy). **(2) S09.** Root cause of the outer-sleeve stripe was the deployed reference sorter promoting the "worn" sleeve crop to a primary reference; renaming that crop's label ($0, data) restored the intended plan; two anchored rolls later ($0.64 billed) S09 has plain upper arms, zip closed, stand collar with the tie knot at the neck. **(3) Native-media QA** (`scripts/qa/native_media_qa.py`): timeline/audio exact; garment residual 2.7–4.1; hook Look anchor-similarity spread 0.185 → 0.097; matte no worse than the master on hook shots, S04 borderline. **(4) Targeted Astra** (new `--mechanism` mode: one part, cross-shot frames, before/after pairs; $1.09 + $0.76): wordmark IMPROVED, matte IMPROVED, S12 stray lettering RESOLVED, S09 accepted once the brief stated the garment truth (shirt collar + tie knot visible inside the standing collar over a closed zip — Astra had scored the anchor's own construction as "open"); **construction and canonical_look UNCHANGED**: S08 split lower fronts + shirt tails, collar facing/height S06 vs S08 vs S11, cuff exposure, tie pattern changes, S01 vs S03 denim texture. Astra: not ready for a full review. **(5) Repairs shipped in v8/v9 ($0):** S12 eraser now catches every frame (nearest-anchor ownership, bridge-breaking island test, 4 anchors); S11's missing wearer-right band half is a deterministic tracked plain band (`--track-expand` lets the tracker use surrounding texture; 6 anchors; ends at the zip). **Next mechanism decision (Fendi/ChatGPT):** best-of-N anchored rolls per shot scored deterministically against the anchor (S08 first, ≈ $1.1–1.7/shot) vs Architecture C landmark propagation vs accept.

**Latest (rev 24 · v6 on the canonical-Look mechanism):** Fendi set a **$50 testing budget** ("make the best decision … otherwise we decide at the threshold") and redeployed `grok-image-garment-proxy` (PR #156: anchor + Look refs). **E2** (14 anchored hero stills on S06, $1.68): `xai:images/edits` takes **at most 3 images** (pinned in `providerCapabilities.ts`, test); stills reproduce the anchor's garment but not the pose — arms move — so they are not pose-locked heroes for pixel propagation (`hero_stills_qa.py`, `propagate_keyframe.py --hero-stills` with RVM silhouette intersection + head exclusion). **E1 is the production mechanism**: set the approved Look-on-artist frame as the Look's `outfit_sheet_path` (hook: `heroes/S06/anchor_hook_s11_f0080.jpg` = S11 v4c f80; pre-hook: `heroes/anchor_prehook_s04_f0060.jpg`) — the deployed proxy already sends it first with the "approved realisation" sentence — and `/videos/edits` reproduces that realisation in every shot ($0.55/shot, no deploy, no code). All 8 slots re-rolled (S09 needed one retry: `v4c-anchor-e1b` adds a negative sentence for the crossed-arm pose). **Brand layer** (`garment_graphic_track.py`): `--anchors` multi-anchor with per-frame owner, `quad_shape_ok` gate (collapsed fits lose the frame), `--erase-above/--erase-below/--erase-extend` island eraser (off-colour blobs enclosed by garment colour are inpainted; anything touching the band edge — tie, hand, stripe — is left alone); wordmark on S06/S08/S09/S11/S12. **v6** = env7 shadow-aware composites + B-roll from the recorded plan → `section_v6.assembly.json`, `section_v6_vs_v5_contact_sheet.jpg`; 720p preview sent to Fendi. Design doc §6 records E1/E2. Next paid step: Astra #4 targeted review (needs OpenAI top-up; ≈ $5–7).

**Latest (rev 23 · propagation test + proxy verified):** Fendi redeployed `grok-video-edit-proxy`; a $0 dry run confirms the build reads the Looks' data (`constraints` composed first, `spec` last, `reference_policy`, `hero_feature_id`; plan shows `referencePlan` = on-model, flat, two detail crops, `providerCapability` xai:videos/edits max 8, first-frame conditioning unverified). Found and fixed on the way: a `detail` crop whose label said "worn" was promoted to on-model by the label heuristic — an explicit `angle` is now authoritative (`_shared/garmentReference.ts` + `src/lib/garment/vtonReference.ts`, test added; 434 tests pass). **Propagation test (ruling §2, $0):** `scripts/edit/propagate_keyframe.py` carries one hero garment across the real S06 footage with chained dense flow (drift-gated confidence, occluders detected on the real footage, hero re-lit from the master, edit frame as the base, nothing invented). Within ±8 frames of the hero the garment is ONE realisation and flicker is 10 % lower than the xAI edit; reach at ≥ 80 % coverage is 6 back / 3 forward, and DIS-chained, DIS-direct and RAFT-small-direct all fall off at the same rate → the motion (≈ 5 px/frame) is the limiter → **re-anchor cadence ≈ 12–16 frames**, as `VIDEO_SWAP_ARCHITECTURE.md` §3 predicted. Multi-hero from the edit (7 heroes / 24 frames) covers 64 % with 25 % less flicker but visible switches, because each hero is a different xAI sample — the heroes must share one realisation, i.e. come from the still lane conditioned on one approved hero (E2, ≈ $0.15 per still, ≈ 10–14 per shot ≈ $2). Verdict vs the kill criterion: viable at that cadence, not viable with one hero per shot. Evidence: `S06_propagation_qa_sheet.jpg`, the two QA JSONs, the half-speed clip sent to Fendi; reasoning in `CANONICAL_LOOK_PROPAGATION_DESIGN_2026-09-21.md` §5. **Decision point for Fendi:** approve ≈ $2 of `/images/edits` heroes for S06 (E2) so the full-shot propagation + brand layer can be built and judged; until then Claude has no $0 step left on this lane. Astra #4 still held.

**Latest (rev 22 · ruling executed, $0):** ChatGPT's architecture ruling on v5 is recorded verbatim in `docs/VIDEO_LANE_CHATGPT_YSL_V5_RULING_2026-09-21.md` (relayed by Fendi). Everything it asked for that needs no spend is on `main`: **(§8)** `GROK_VIDEO_EDIT_PROMPT_V4C_FULL_LOOK` + `composeConstraintsFirst()` in the registry (tests pin the exact runtime text). **(§6)** `render_broll_slots.py` is two stages — `--plan` resolves the recipe (explicit or proposed from the ShotSpec) into `generation.parameters.broll` in the ShotSpecs, render executes only what is recorded and fails closed otherwise; the seed script preserves the block on re-seed; the section's four B-roll shots are planned and the render is decoded-md5-identical to v5. **(§3)** `_shared/lookReferences.ts` implements the Look truth hierarchy (outfit sheet → hero primary refs → hero detail refs → other pieces → structured spec), `reference_policy` resolves artist < project < Look < request, `constraints` on the Look are composed FIRST into the prompt server-side and the Look `spec` LAST (both recorded in the plan); `_shared/providerCapabilities.ts` holds the reference ceiling (overridable via the `PROVIDER_CAPABILITIES_JSON` secret, no deploy; safety ceiling 8 in code until the provider limit is verified). Both Looks now carry `hero_feature_id`, `reference_policy`, `constraints`, `spec` as DATA (written through the app, no deploy). **(§7)** the review builder states an EVIDENCE BOUNDARY on every part (`UNOBSERVABLE` list), questions 11/12 no longer ask for musical judgement, the aggregator surfaces `_native_media_qa_required`. **(§2)** `docs/CANONICAL_LOOK_PROPAGATION_DESIGN_2026-09-21.md`: the xAI `/videos/edits` contract documents NO first-frame/canonical conditioning (`reference_images` is accepted but undocumented); `/videos/generations` has pinned-first-frame + references but regenerates the performer (fails the lane); `/images/edits` (≤ 5 images) is the hero-still generator; the propagation lane (locked Architecture C) can be built and tested at $0 on the v5 edits with the new tracking core. Two costed experiments (E1 ≈ $1.10, E2 ≈ $0.15/still) are written up, NOT run. **(§4)** `scripts/edit/garment_track.py` (tracking core: LK tracks with true anchor coordinates, affine/similarity/homography fit, coast + NCC/LK re-acquisition against the anchor, appearance-residual occlusion with blob filtering, illumination model, synthetic tests) and `scripts/edit/garment_graphic_track.py` (canonical graphic → tracked plane → perspective warp → colour match + footage shading → occlusion → composite → temporal QA). First real run on S08 v4c: the product-photo wordmark tracks the band through the arm crossing (jitter 1.3 px, 0 frames lost, hidden behind the sleeve); evidence `S08_wordmark_track_qa_sheet.jpg` + clip sent to Fendi. Known limit: the generator draws the band longer than the true one, so a stub of its lettering survives past the patch in a few frames — reported, not hidden. **(§5)** `composite_environment.py --refine shadow`: in the uncertain band only, a local attenuation model of the background (the shadow), a structure test (local NCC with the background — a shadowed door keeps the door's lines, a cream jacket does not), and known-background unmixing with RVM's fgr; the confident core is never touched. S11 edges lose the door halo without eroding the cap/face; S12/S08 rendered for the record (`env6/`). **Held:** Astra #4 (§1/§10) until one of these mechanisms produces new evidence in an assembled draft. **Fendi:** redeploy `grok-video-edit-proxy` (§9; the version on main is what the Looks' new data expects), spend approval only if E1/E2 are wanted.

**Latest (rev 21 · full outfit + loop #3):** Fendi: "the outfit swap is only placing the jacket on me… it should be in AVT" → the video lane now takes a composed **Look** (`artist_looks.composition_recipe_json.wardrobe_feature_ids`) via `grok-video-edit-proxy` `lookId` + `referenceMode:"full_look"`, one reference per piece, hero piece with on-model + flat + detail crops. **Reference policy is data** (request `referencePolicy` > Look recipe `reference_policy` > defaults; code keeps only the provider cap) — Fendi's rule after a redeploy was needed for a reference-count constant: "an outfit update should populate in the tool in a seamless workflow" (**needs one redeploy to land, then none**). Looks: pre-hook *Noir Hook 2* (`7e7760e6`), hook *White Ice — Full Outfit* (`ce7f6350`, jacket + SL trousers). Fendi's garment truth (with ysl.com crops attached to the jacket feature as `detail` refs): stripe INSIDE the sleeves, STAND collar navy-inside/mastic-outside, jacket ZIPPED; glasses not shades; construction facts go FIRST in the prompt (`V4B/V4C` — hit rate 1/5 → 4/4). 17 edits this round ($8.24): pass 1 got the whole outfit on him in 8/8 slots (one short-sleeve re-roll), passes 2–3 fixed zip/collar/sleeves on the hook. **Astra #3 on v5: REPAIR_REQUIRED, 5/5/8/3/5/6** — nothing marked resolved: the outfit is right but every shot is a different approximation (cuffs, hem, lettering); Astra holds zero-deviation and that ceiling is the generator's. **Generalization done (Fendi: "no patches or hard coding for this project only"):** `scripts/edit/render_broll_slots.py` (data-driven, replaces `ysl_section_fx.py`; per-section `broll_recipes.json`), `build_astra_review_package.py` templated on a `treatment` block in the ShotSpecs (artist, brand, direction, source environment), output budget on every part (two `max_output_tokens` incompletes cost ≈ $4.6). Next lever: hero-frame-conditioned generation per Look + deterministic wordmark repair; matte: shadow-aware refinement. Details: result doc §"Full-outfit pass" and §"Review #3".

**Latest (rev 20 · Astra loop):** Astra is now the visual-review layer for completed drafts and the loop has run twice on the real render. Infra (all merged): `supabase/functions/astra-visual-review-proxy` (user-JWT-only bridge to OpenAI `gpt-6-astra`, background submit/poll because the edge gateway kills requests at 150 s, fail-closed cost gate, result stored under `project-exports/…/astra-reviews/`), `src/lib/qa/astraVisualReview.ts` (Zod contract: per-shot EXPECTED/OBSERVED/VERDICT, transitions, sequence defects with stable `defect_id` + `recommended_owner` routing, `final_verdict`, `escalate_to_fendi`; `diffReviews`), `scripts/qa/build_astra_review_package.py` (the draft mp4 → timestamped frame strips at 3 shot groups / ±0.25 s cuts / 2 fps sequence, the full intent package as text, garment/identity/source references; `--prev-review` hands Astra the previous defect ids so identity survives revisions; `--repair-notes` as claims to verify; sample times snapped to frame pts), `scripts/qa/astra_review_runner.browser.js` (runs in the authenticated AVT tab), `scripts/qa/aggregate_astra_review.py` (parts → `AstraReviewSchema` + `_diff_vs_prev`), `docs/qa/ASTRA_VISUAL_QA.md` (loop, routing table, escalation rules, run steps, lessons). **Review #1** on `YSL_IceOn_bars24-46_v1`: REPAIR_REQUIRED, 44 defects, 7 blockers — all matte (U²-Net dropped legs/arms, closet leaked). **Repair:** matte → **RobustVideoMatting** (video matter, ONNX, CPU 7 fps) in `composite_environment.py`; new hook plate (several mirror panels + glints); S04 1.25× crop in the assembler (read from the ShotSpec); S05 diamond insert, S10 corridor strobe, S02 locked macro in `ysl_section_fx.py`; Look 1 regenerated with the **reference-true** prompt (`v3c` — the old prompts described a wide band + centred white lettering; the flat reference has ONE narrow stripe + small gold wearer-left mark). **Review #2** on `_v2`: REPAIR_REQUIRED but **20 resolved, 0 blockers**, scores 4/4/8/3/4/5 → 6/6/8/4/6/7; Astra on Q15: "structurally a real Fendi performance enhanced by AVT — not a replacement-performer montage; visually it still advertises the AI process (garment variants, malformed branding, source leaks)". **Repair → v3 (committed, not yet re-reviewed):** matte v3 (RVM foreground decontamination at the fringe, alpha remap, static-pixel peel of attached closet fragments). **Blocked:** xAI refused further wardrobe runs — `permission-denied: team 1a20564d… has either used all available credits or reached its monthly spending limit` — so S11 still wears the v1 garment and the S08/S12 collar + S09 sleeve re-rolls did not run; garment construction also varies shot-to-shot because each xAI edit is an independent sample (Astra: `temporal_propagation`). Persisting owners: `wardrobe_generation`, `temporal_propagation`, `brand_repair` (no deterministic wordmark repair for moving footage yet), `environment` (pre-hook mirror reads as a slab — aesthetic call), `sync` (frame strips cannot certify one-frame lip sync; native-rate pass needed). Full loop record: result doc §"Astra visual-QA loop" + `astra/` folder. **Full outfit, not just the jacket (Fendi, 2026-09-21, "it should be in AVT"):** the lane so far replaced ONLY the jacket (`pickGrokVideoEditReferencePaths` = flat photo, max 1; prompt keeps trousers/shoes). The outfits exist in AVT as composed Looks (`artist_looks.composition_recipe_json.wardrobe_feature_ids`: e.g. *Look B — Noir Hook 2* = trucker + Mick Long Jeans + glasses; *White Ice* = track jacket only; the campaign shot with striped shirt + tie + black pleated SL trousers is the jacket's on-model reference and *SL look — trousers*). Repo now (needs redeploy, no spend): `grok-video-edit-proxy` accepts `lookId` (expands the Look to one flat reference per piece, on-model too for the first piece under `referenceMode: "full_look"`, ≤ 4 refs, `look_id` + `reference_mode` recorded in `metadata_json`), and `GROK_VIDEO_EDIT_PROMPT_V4_FULL_LOOK` (jacket open over striped shirt + striped tie, black pleated trousers; cap/glasses/shoes stay Fendi's own until he rules). Waiting on Fendi: which Look is the hook outfit (White Ice + SL trousers? shirt/tie?), cap+glasses vs the campaign shades; and the xAI limit. A full-outfit pass over all 8 performance slots ≈ $4–5. **Do not scale to the full song**: the section has not passed the loop.

**Latest (rev 19 · takeover):** The first real section of the YSL video exists: `YSL_IceOn_bars24-46_v1` (song 47.213–90.492 s, 1080×1920 @ 24, album-master audio). Sync was **measured**, not assumed: `song = master + 0.8538 s` (GCC-PHAT, 23/30 windows, PR #148 merged: `performance_syncs` + `timeline_items.source_*` additive migration — **not yet applied live** — `src/lib/sync/performanceSync.ts`, `scripts/sync/align_song_performance.py`, `docs/SYNC_SONG_PERFORMANCE.md`). Grid measured 122.00 BPM (the 140 seed was a hypothesis). Treatment = 12 ShotSpecs on the song clock with resolved master ranges (`scripts/seed-ysl-section-treatment.ts` → `docs/treatments/ysl-ice-on.section-bars24-46.shotspecs.json`). **Wardrobe on moving Fendi works**: xAI `/v1/videos/edits` through the authenticated `grok-video-edit-proxy`, flat product photo as the single reference, prompt `v3-jacket-only` (registered as `GROK_VIDEO_EDIT_PROMPT_V3_JACKET_ONLY`) for the Saint Laurent track jacket (hook, bars 32–46) and `trucker-v1` for the YSL trucker (pre-hook, bars 24–32). 8/8 slots passed Gate 0 (identity, garment on body, photoreal next to master, anatomy, framing, garment truth), temporally stable, frame-aligned to source from t=0 (xAI trims ~0.26 s off the tail; 0.5 s handles absorb it). One retry (S08 band broken), one conditional (S12 wordmark oversized). **Environment**: a second v2v pass over Fendi was tested once and **rejected** (face relit into darkness); the environment is now a deterministic composite — U²-Net person matte with a static-background prior + temporal median over Grok Imagine "cold fitting room" plates (`scripts/edit/composite_environment.py`). **B-roll/FX** all on the song clock from real material where possible (`scripts/edit/ysl_section_fx.py`): S02 real macro of the trucker, S05 ice-hit white-out across the look change into the drop, S07 night-city insert (Grok Imagine still, animated), S10 mirror-flip strobe. **Assembly** places every render by `songToPerformance(t) − masterStart` with exact per-slot frame budgets and cuts the WAV for the same range (`scripts/edit/assemble_section.py`) — no filler is ever accepted. Full result, Gate-0 log, ledger, generalized fixes and limitations: `docs/research/results/2026-09-20-ysl-real-video-1/YSL_REAL_VIDEO_1_SECTION_BARS24-46_RESULT_2026-09-21.md`. **Product bug fixed in repo (needs redeploy):** the proxy billed before validating `shotId` (uuid column) — now fails closed (`invalid_shot_id`). **Open for Fendi:** apply migration `20260920120000` (Lovable SQL editor, additive); approve `~/Downloads` on the linked Mac so Grok Imagine *videos* can be retrieved (assets.grok.com 403s the cloud sandbox; only still images are on the public CDN); decide whether to scale to the full song (≈ $16–20 in edits, inside the $50 cap).

**Do not:** treat `f31bd0f2` (V2 clip, shirt+tie) as a usable Look-1 render — S09 has its own cut+edit (`8db0030f`); re-run the v2v environment pass; mint asset rows with `shotId` labels (use `metadata_json.treatment_shot_id`).

**Earlier (revs 3–18):** Architecture C still-repair stages 1b→1l (Cursor, ChatGPT rulings; 1l NOT CLEARED 10/11, C9-right) — history preserved below; superseded for production by the moving-footage lane above (the still-repair chain never reached Gate-0 quality; the video-edit lane did).

**Rev 18 (Cursor):** Stage 1l live asset **`9eaf0c55-5fdd-44ac-ac5c-9a9a86414c75`**, `repair_method_version: architecture_c_still_repair_1l`, `occlusion_source: sam3`, `sam3_ok: true`, fallback `false`, still `2aa1a44c`, keyframe `v2-still-0.785`, canonical chest quad `[[0.30,0.530],[0.87,0.533],[0.87,0.585],[0.30,0.582]]`, `effective_band_bbox.pixel_count` 27 390. Lane E unfiltered mid-luma (`bandAuthorityMaskUsed: false`). **Verdict: CHEST STILL GATE NOT CLEARED — 10 / 11** (PASS 1, 2, 3, 4, 5, 6, 7, 8, 10, 11; FAIL **9 only**). vs 1k `c9c4efee` (7/11): **C2 remnants 6→0**, **C4 cream→navy 19/1-px→0**, **C6 right-end 41→0**; C9 improved 0.195→0.172 (left 0.042→0.000, right 0.273→0.261). C9-right (x 462–500 / y 713–723; 84/488) remains the gate. Repair was **not** re-run; paint was **not** changed. Identity + UI path: `ARCHITECTURE_C_STILL_REPAIR_STAGE1L_LIVE_ASSET_2026-09-15.md`. Scorecard: `ARCHITECTURE_C_STILL_REPAIR_STAGE1L_RESULT_2026-09-15.md`. Do not mint a second 1l row.

**Latest (rev 17):** Stage 1l live asset **`9eaf0c55-5fdd-44ac-ac5c-9a9a86414c75`** identity only (PR #69). Same SAM-3 / still / quad / bbox as rev 18. **Gate then: NOT SCORED** (Lane E follow-up, now completed in rev 18). Cloud VM anon POST remains 401. UI path + one-shot script: `docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1L_LIVE_ASSET_2026-09-15.md`.

**Latest (rev 16):** Stage 1k live asset **`c9c4efee-6bd2-450f-a9e4-b70fb9b722bb`**, `repair_method_version: architecture_c_still_repair_1k`, `occlusion_source: sam3`, `sam3_ok: true`, `sam3_reason: null`, `allow_skin_heuristic_fallback: false`, `effective_band_bbox.pixel_count` 27 307, latency ~56.6 s. Lane E unfiltered mid-luma (`bandAuthorityMaskUsed: false`). **Verdict: CHEST STILL GATE NOT CLEARED — 7 / 11** (PASS 1, 3, 5, 7, 8, 10, 11). vs 1j `fb8117ee` (5/11): **C5 sleeve 4×3 fixed** (8→0), **C8 tapes painted** (47/68 bright→0). Still FAIL: **C2** 6 pinstripe remnants (1j 22); **C4** 19 cream→navy / 1-px raise at x 290 (1j 71 / 3-px); **C6** right-end **41** cream→navy (1j 109; fixture 42); **C9** ghost ratio **0.195** (left 0.042, right 0.273) vs ceiling 0.05 (1j 0.590). Fixture predicted 9/11 (FAIL C2+C6 only) — live extra FAILs are C4 and C9-right (wordmark half). 1k vs 1j: 3 773 px. 1i occlusion + 1j ROI hold. Score-only; repair not re-run.

**Latest (rev 15):** Stage 1j (Cursor `88af4fa`; `main` @ `2dfc33e` after two Lovable-agent type-annotation commits to the α fixture, `73c63fc`/`2dfc33e`, harmless) redeployed by Fendi twice — the first redeploy still served `_1i` and was refused as DEPLOYMENT MISMATCH on 09-11; the second is **confirmed live by behaviour** → asset **`fb8117ee`**, `repair_method_version: architecture_c_still_repair_1j`, `occlusion_source: sam3`, `sam3_ok: true`, `sam3_reason: null`, `allow_skin_heuristic_fallback: false`, `effective_band_bbox.pixel_count` 27 582. **Compute fixed:** single attempt, ~10.8 s (1i: 546 then ~41 s). **Verdict: CHEST STILL GATE NOT CLEARED — 5 / 11** (PASS 1, 3, 7, 10, 11). **1j vs 1i: only 384 px differ.** Occlusion ownership preserved (crease coverage 1.0, wedge 0.81–1.0, skin byte-identical). **A — mid-luma interior: 595 / 866 still unpainted** (1i 809): the ≥ 4-original-seed-neighbour rule keeps only AA pixels bordered by seeds on ≥ 4 sides; a glyph's AA ring has navy seeds on one side and the glyph's *close-added* bright core on the other (2–3 seed neighbours) → still rejected; same for ridge AA and tape edges. Outlines remain visible on both halves (60 % / 59 % of mid-luma pixels in the two lettering windows above median + 20). **Unchanged:** pinstripe fragments rows 676/679 (byte-identical to 1i), cream-body → navy 97, right-end protrusion 114 px (x 580–616 / y 713–730). **Improved:** diagonal tape mostly absorbed (dotted remnant), left tape x 399–401 survives. **Minor regression:** 8 cream px at x 389–392 / y 746–748 darkened (same rule admitting boundary cream with ≥ 4 dark neighbours). **Test-model mismatch [V]:** golden suite run locally at `88af4fa` → 38 / 38 pass, including "real-crop mid-luma interior AA is painted" — because it counts only pixels with `bandAuthorityMask ≥ 0.5`, i.e. it skips exactly the pixels the rule rejects; the same windows without that filter give ghost ratios 0.60 / 0.59 vs the test's `< 0.25`. The "right-end protrusion" test asserts x 373–399 / y 745–759 (the sleeve block), never x 580–616. Evidence committed as authorized (`d3dbe64`).

**Latest (rev 14):** Stage 1i (Cursor `fb1d996` + `cc5b796`, PR #49; `main` @ `1ada136`) redeployed by Fendi (edge only) and **confirmed live by behaviour** → asset **`21972fcf`**, `repair_method_version: architecture_c_still_repair_1i`, `occlusion_source: sam3`, `sam3_ok: true`, `sam3_reason: null`, `allow_skin_heuristic_fallback: false`. **First attempt returned HTTP 546 `WORKER_RESOURCE_LIMIT`** (no asset); the identical second request succeeded in ~41 s — 1i's two full-frame r = 12 dilations in `applyChestLocalOcclusionSemantics` push the edge worker to its compute limit (availability risk, not an output defect). **Verdict: CHEST STILL GATE NOT CLEARED — 6 / 11** (PASS 1, 3, 5, 7, 10, 11). **Occlusion ownership changed as intended [V]:** crease implied coverage `1.0` across x 276–288 (1e–1h `0.84…0.39…1.0`), wedge `0.75–1.0` (1d–1h `0.00`), `effective_band_bbox.pixel_count` 25 347 → 27 540, hand/forearm skin byte-identical. **Fixed vs 1h:** crease gone (column means 33 = band); wedge absorbed (region mean 17.8 → 42.9); top-edge raise on the wearer's-left gone, 2-px feathered ramp, no light seam; sleeve block/spill/hairline gone (0 darkened px); cream-body → navy 765 → 97. **Still failing:** **2** pinstripe fragments (row 676 x 208–213 / 256–262; row 679 x 228–239 / 267–268; outline curve x 260–280 / y 676–690); **4** 97 cream px at x 280–330 / rows 673–676 (3-px raise at x 290); **6** right-end protrusion x 580–616 / y 713–730 (113 px, unchanged since 1g); **8** wedge absorbed but **both zip tapes survive** (left x 399–401, diagonal x 415–431 — the zip overlay strip is only x 418–424, so it is not the restorer); **9 REGRESSION in kind** — old lettering, pinstripe and tapes re-exposed as bright **outlines** on both halves of the band: 882 unpainted non-candidate interior px (1h 318, 1g 146), **809 of 866 mid-luma (60–180) source px unpainted while bright cores are painted**. **Cause [V]:** `_shared/logoComposite.ts` 1829–1848 rejects every close-added non-navy pixel with luma ≤ 180 — the comment scopes it to shadowed sleeve cream, the code has no spatial/chroma condition — so glyph, ridge and tape antialias edges become holes; tape cores are then orphaned islands dropped by `largestOverlappingComponent` [H]. **Test-model mismatch [V]:** the 1i golden runs the full paint → illumination → zip → chest-local α → composite chain on the real crop (crease/wedge/hand agree live), but its lettering and ridge tests *plant* pure-bright pixels instead of reading the crop's antialiased glyphs, so the outline failure is invisible to CI; no assertion covers the tapes at x 427–431 or the right end x 580–625. Evidence committed as authorized (`c6e032b`).

**Latest (rev 13):** Stage 1h (Cursor `bd4262d`, PR #48; `main` @ `9b5f90c`) redeployed by Fendi and **confirmed live by behaviour** → asset **`39c4a842`**, `repair_method_version: architecture_c_still_repair_1h`, `occlusion_source: sam3`, `sam3_ok: true`, `allow_skin_heuristic_fallback: false`. **Verdict: CHEST STILL GATE NOT CLEARED — 4 / 11** (PASS 1, 7, 10, 11). **Improved vs 1g:** left top edge raise 5–7 → 0–2 px; cream-body → navy 1 386 → 765 px; right-end protrusion 242 → 118 px. **Unchanged:** crease column (x 277–283 col means 27 24 20 18 18 20 25), tie/zip wedge byte-identical, 5-px top-edge raise on the wearer's-left (x 380, 470–590), navy block over the sleeve (x 373–399 / y 745–759). **Regressed:** upper-left pinstripe is back as two hard lines (y 676 x 214–255 luma ≤ 254; y 679 x 214–239); old lettering + armhole pinstripe re-exposed on the wearer's-left under the wordmark (28 → 204 residue px, x 448–566, y 714–734). **Key finding [V]:** implied paint coverage at the crease (`0.84 0.70 0.56 0.43 0.39 0.43 0.56 0.71 0.87 1.0` across x 276–288) and at the wedge (`0.00`, x 402–423) is **identical in 1d, 1e, 1g and 1h** — including 1e, whose quad paint was unconditional. The only stage-invariant post-paint operation is `applyOcclusionAlphaComposite` with the SAM-3 α: the outfit mask has a partial hole along the fold (α ≈ 0.4) and a full hole over the shirt/tie wedge (α = 0). **No paint, clamp, gain-floor or zip-overlay change can fix criteria 3 or 8** — 1g's gain floor and 1h's zip rewrite targeted the wrong layer, and my 1g attribution of the crease to illumination was wrong (correction recorded in the 1h doc). **Lettering regression cause [V]:** 1h's new hard lock `y > bandMidY (713.6) && luma > 180 → never paint`; the wearer's-left band sits lower than the quad, so its lettering row is at y 714–734 — every residue starts at y = 714. Pinstripe regression: 3-px ridge cap < 4–5 px ridge; antialias row (luma 104–129) falls between the absorb (> 140) and candidate (< 60) gates. Top-edge raise: cap counts only newly-claimed px per walk, deeper pixels re-climb → 5 px; `quadTop − 3` guard never binds on the right where the band top is 10–20 px below the quad. **Rulings needed from ChatGPT before code:** (1) occlusion semantics — inside the band component, α must gate hands/face only (`1 − dilate(hands ∪ face)`), not "outfit"; (2) withdraw the luma > 180 hard lock. Evidence committed as authorized (`df64344`).

**Latest (rev 12):** Stage 1g (Cursor `aa595cf` + `b1f0022`, PR #47; `main` @ `81580f7` after three Lovable-agent compile-restoration commits) redeployed by Fendi and **confirmed live by behaviour** → asset **`2d110d13`**, `repair_method_version: architecture_c_still_repair_1g`, `occlusion_source: sam3`, `sam3_ok: true`, `allow_skin_heuristic_fallback: false`, `temporalTrackingEnabled: false`. **Verdict: CHEST STILL GATE NOT CLEARED — 5 / 11.** PASS: 1 full band coverage (left third finally painted), 2 pinstripe removed (rows 672–691 max luma ≤ 58), 7 wordmark, 10 occlusion (skin untouched), 11 outside-region (0 px above y600/below y800). FAIL: **3** crease survives at x 277–283 (band-row column means 17–26 vs band 33); **4** band top edge raised 5–7 px into cream body along its whole length (x 290–590; 1 386 cream-body px → navy); **5** grey smear onto the cream sleeve at the crossed forearm (rows 743–755, x 330–380: 190–199 vs clean 205–216) plus 10 rows of paint below the quad; **6** hard unfeathered staircase along the raised top edge (`224 → 32` in one pixel), ragged protrusion with grey fringe at the band's right end (x≈590–620); **8** tie/zip wedge byte-identical to source (two tapes remain); **9** flat patch, armhole fold shading lost, tonal step at the crease. **Causes traced on `main` (`_shared/logoComposite.ts`):** `absorbTopPinstripeLocal` claims *any* luma > 140 within 6 px up the normal (no thin-ridge test) → cream body absorbed everywhere; α re-asserted to 1 on `coreComponent ∪ stripeAbsorb` cancels the inward feather exactly at the outer boundary; expansion pass admits bare `luma < 140` → shadowed sleeve painted; `applyLowFrequencyBandIllumination` gain floor 0.80 re-imposes the source crease (33 × 0.80 ≈ 26 = measured); `overlayZipFromSource` restores both source tapes. Real-crop golden fixture passed because nothing asserts cream rows 662–676 unchanged, an outer-edge ramp ≥ 2 px, or crease ≥ median − 6 after illumination. **Recommended (bounded, not a new mechanism):** (a) absorb only thin bright ridges (ray must return to non-cream within ~3 px); (b) re-assert α = 1 only on `erode(core ∪ absorb, featherPx)`; (c) drop bare `luma < 140` from expansion; (d) illumination gain from a crease-masked band; **ruling for ChatGPT:** one canonical zip line at the left tape (x≈400) with the band covering the diagonal tape, or keep the wedge as generation debt. Full doc + six evidence crops committed at Fendi's request (`77ca504`).

**Earlier (revs 3–11, 09-04 → 09-07):** 1b/1c/1d/1e/1f scored NOT A PASS in turn — wordmark fixed (1b), band regressed then recovered (1c/1d), 1e deterministic, 1f exposed `isNavyPixel` as the paint authority (left third unpainted; the x 279–291 "seam" is a source crease). Full history in the dated result docs listed below and the 1e/1f directive docs.

## Read this

**`docs/VIDEO_LANE_CHATGPT_YSL_V5_RULING_2026-09-21.md`** — ChatGPT's ruling on v5 (verbatim) and Claude's execution table. **`docs/CANONICAL_LOOK_PROPAGATION_DESIGN_2026-09-21.md`** — provider contract check (what xAI's edit/generation/image endpoints do and do not condition on), the two costed experiments, the propagation lane as it can be built at $0.

**`docs/handoffs/CHATGPT_REVIEW_YSL_SECTION_V5_2026-09-21.md`** — the review brief for ChatGPT (requested by Fendi 2026-09-21): what to review in PR #153, the loop verdict and where the ceiling is, Astra's escalations, and the four design questions (hero-frame-conditioned Look propagation, wordmark repair on moving footage, matte direction, stop rule) that decide the next round.

**`docs/qa/ASTRA_VISUAL_QA.md`** — the review loop (Astra = reviewer, not creative director), tooling constraint (text + images only → frame strips), contract, routing table, escalation rules, run steps, the loop log for this section and the lessons (defect identity needs the previous list; repair notes are claims; output budget; sampling artefacts). **`docs/research/results/2026-09-20-ysl-real-video-1/astra/`** — `astra_review_v1.json`, `astra_review_v2.json` (with `_diff_vs_prev`), per-part results, `repair_notes_v2.txt`, `pkg_v2_manifest.json`.

**`docs/research/results/2026-09-20-ysl-real-video-1/YSL_REAL_VIDEO_1_SECTION_BARS24-46_RESULT_2026-09-21.md`** — the takeover result: mandate-test table, Gate-0 log per shot with asset ids and cost, ledger, problems → generalized fixes, limitations, scaling recommendation. Evidence in the same folder (`S06_wardrobe_src_vs_edit.jpg`, `S01_trucker_zoom.jpg`, `S08_attempt1_rejected_band_broken.jpg`, `S06_env_v2v_rejected.jpg`, `S06_env_composite.jpg`, `env_composites_*.jpg`, `fx_*.jpg`, `section_v1_contact_sheet.jpg`, `alignment_check.json`, `section_v1.assembly.json`, `renders_all.json`).

**`docs/SYNC_SONG_PERFORMANCE.md`** + `docs/sync/ysl-ice-on.performance-sync.json` — the canonical sync record and how the edit uses it. **`docs/treatments/ysl-ice-on.section-bars24-46.shotspecs.json`** — the executable section treatment (status `generated` on the 8 performance shots, output asset ids in `generation.parameters.wardrobeOutputAssetId`).

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1L_RESULT_2026-09-15.md`** — 1l live verification: preflight (`9eaf0c55`, `_1l`, sam3 ok), Lane E **NOT CLEARED 10/11** vs 1k 7/11 and fixture 11/11, forensic A–E. C9-right remains. Evidence `stage1l_chest_compare.jpg`, `stage1l_pinstripe_topleft.jpg`, `stage1l_crease_lettering.jpg`, `stage1l_right_top_edge.jpg`, `stage1l_sleeve_zip_bottom.jpg`, `stage1l_centre_wedge.jpg`, `stage1l_lane_e_report.json`, `stage1l_live_score.json`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1L_LIVE_ASSET_2026-09-15.md`** — 1l live identity (`9eaf0c55`, `_1l`, sam3 ok). UI click path + one-shot script. Do not mint a second 1l row.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1K_RESULT_2026-09-15.md`** — 1k live verification: preflight (`c9c4efee`, `_1k`, sam3 ok), Lane E 7/11 table vs 1j 5/11 and fixture 9/11, forensic A–E, ImageScript decoder note. Evidence `stage1k_chest_compare.jpg`, `stage1k_pinstripe_topleft.jpg`, `stage1k_crease_lettering.jpg`, `stage1k_right_top_edge.jpg`, `stage1k_sleeve_zip_bottom.jpg`, `stage1k_centre_wedge.jpg`, `stage1k_lane_e_report.json`, `stage1k_live_score.json`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1J_RESULT_2026-09-14.md`** (`d3dbe64`) — 1j verification: preflight (incl. the refused 09-11 mismatch run), causal checks A–E, eleven-criterion table with vs-1i status, per-defect causal layer, the local golden run and the exact reason the AA test cannot fail, and the recommended reference-set fix. Evidence `stage1j_chest_compare.jpg`, `stage1j_pinstripe_topleft.jpg`, `stage1j_crease_lettering.jpg`, `stage1j_right_top_edge.jpg`, `stage1j_sleeve_zip_bottom.jpg`, `stage1j_centre_wedge.jpg`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1I_RESULT_2026-09-10.md`** (`c6e032b`) — 1i verification: runtime preflight incl. the 546, causal checks A–E, eleven-criterion table with vs-1h status, per-defect causal layer with line references, the regression/evidence audit table (what CI exercises vs what live disagrees with), and the scoped recommendation. Evidence `stage1i_chest_compare.jpg`, `stage1i_pinstripe_topleft.jpg`, `stage1i_crease.jpg`, `stage1i_right_top_edge.jpg`, `stage1i_sleeve_zip_bottom.jpg`, `stage1i_centre_wedge.jpg`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1H_RESULT_2026-09-10.md`** (`df64344`) — 1h verification: runtime gate, 1h-vs-1g delta, eleven-criterion table, causal layers A–E with the implied-coverage proof that the SAM-3 α owns the crease and wedge, the hard-lock trace for the lettering regression, the two rulings, bounded corrections, and golden additions (dump the live α into the real-crop fixture). Evidence `stage1h_chest_compare.jpg`, `stage1h_pinstripe_topleft.jpg`, `stage1h_crease.jpg`, `stage1h_right_top_edge.jpg`, `stage1h_sleeve_zip_bottom.jpg`, `stage1h_centre_wedge.jpg`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1G_RESULT_2026-09-10.md`** (`77ca504`) — 1g verification: metadata, eleven-criterion table with probe numbers, per-defect causal layer with the exact code path, corrections (a)–(d), the zip ruling, golden-fixture additions, and the Lovable-agent commit review. Evidence `stage1g_chest_compare.jpg`, `stage1g_top_edge.jpg`, `stage1g_left_zoom.jpg`, `stage1g_forearm.jpg`, `stage1g_centre.jpg`, `stage1g_right.jpg`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1F_RESULT_2026-09-07.md`** (`caf5b12`) — 1f verification: metadata, ten-criterion table with probe numbers, classifier measurement table, root cause with file/function names, why the golden suite missed it, corrections C1–C4, and the tie-wedge decision. Evidence `stage1f_navy_classifier_map.jpg`, `stage1f_chest_compare.jpg`, `stage1f_left_zoom.jpg`, `stage1f_topleft_edge.jpg`, `stage1f_forearm_boundary.jpg`, `stage1f_zip_column.jpg`.

**`docs/ARCHITECTURE_C_CHATGPT_STAGE1F_DIRECTIVE_2026-09-05.md`** — the ruling Cursor implements, with Cursor's concurrence and Claude's verification plan (pixel probes + targets).

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1E_RESULT_2026-09-05.md`** — 1e verification: full metadata, criterion-by-criterion table, pixel-level defects D1–D3 with smallest corrections (inward feather + 2 px navy dilation; `navy ∪ (quad ∩ dilate(navy,4px))` instead of bare union; single paint pass + luma clamp). Evidence `stage1e_chest_compare.jpg`, `stage1e_zoom_annot.jpg`, `stage1e_topleft_edge.jpg`, `stage1e_forearm_boundary.jpg`, `stage1e_zip_column.jpg`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1D_RESULT_2026-09-05.md`** — 1d scorecard + five corrections (paint region = navy-band mask ∪ quad; feathered alpha; trace black speckles; illumination gain; exercise the 422 path once). Evidence `stage1d_chest_compare.jpg`, `stage1d_zoom_annot.jpg`, `stage1d_left_zoom.jpg`.

**`docs/research/results/2026-09-04-grok-build/GROK_BUILD_CONSUMER_EXPERIMENT_2026-09-04.md`** (`3365cb9`) — Fendi-directed side experiment on grok.com Build Mode (consumer plan, $0 API). Grok could not do video-to-video; it did `imagine_reference_to_image` (source frame + on-model + flat) → image-to-image fix → image-to-video. **Garment construction is the closest to zero-deviation seen in this project (A, B, D, E, F, I, J all correct in one pass), but the output is a regeneration of Fendi — FAIL for the product lane.** Open question for ChatGPT/Cursor: is that image-edit-with-references model reachable via the xAI API? If so it is a hero-keyframe/anchor candidate for Architecture C. No gates touched.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1C_RESULT_2026-09-04.md`** — the 1c scorecard on `8ebfe83` with six corrections (defect-masked low-frequency shading clamped ±15 %; expansion along the band normal only; zip as feathered overlay; SAM-3 garment mask with `occlusion_source` surfaced; mask-derived quad; golden regression test). Evidence `stage1c_chest_compare.jpg`.

**`docs/research/results/2026-09-04-still-repair/ARCHITECTURE_C_STILL_REPAIR_STAGE1_RESULT_2026-09-04.md`** (`33e2ab9` + two addenda) — full result, disclosure of how the quad was supplied, defect table, and six proposed fixes. Annotated evidence in the same folder (`stage1b_ref_vs_result.jpg`, `stage1b_before_after.jpg`, `stage1b_chest_zoom.jpg`, `v2_pockets_cuffs_annot.jpg`).

Prior context: `docs/ARCHITECTURE_C_V2_DEFECTS_AND_PROPOSED_FIXES_2026-09-03.md` (register A–F) and `docs/ARCHITECTURE_C_CHATGPT_V3_STILL_FIRST_RULING_2026-09-03.md` (sequence).

## Disclosure (closed)

The first stage-1b run supplied the quad directly to `architecture-c-still-repair-proxy` because the live app pre-dated `4af7afa`. After Publish, the same quad was entered in-product and the UI-path output is byte-identical to the proxy-path output (addendum 2 in the result doc). No open disclosure.

## Open items by owner

| Owner | Item |
|---|---|
| **Fendi** | (1) Ruling on the S08 smoke (≈ $4.5): go / xAI-only best-of-N / hold. (2) **Redeploy AVT `runway-video-edit-proxy`** (rev 27 fixes the 503). Control Center is already deployed; `proxy-provider-call` is unchanged by the fix and needs no redeploy. **Do NOT add `RUNWAY_API_KEY` to AVT** — provider execution moved behind Control Center, which already holds the key. (3) Redeploy `grok-video-edit-proxy` + `grok-image-garment-proxy` when convenient (per-model capabilities, prompt-length fail-closed, sorter fix); optionally set `XAI_IMAGE_EDIT_MODEL=grok-imagine-image-2.0` (5 refs, cheaper, the model that replaces ours on 2026-11-02). (4) OpenAI ≈ $4.1 left — top up before any full Astra review. |
| **Claude** | On the go: run the smoke (E1 ×2, Aleph 2.0, Gemini Omni Flash), score with `construction_score.py`, one targeted Astra part, apply the kill criterion, report. If xAI-only: S08 best-of-3 ($1.68) with the same scorer. Matte lane unchanged. Not scaling to the full song. |
| **ChatGPT** | Review `CANONICAL_LOOK_PROPAGATION_DESIGN_2026-09-21.md` §4 (order of the three steps) and the tracking core's design choices (affine default, residual-blob occlusion, NCC re-acquisition). Still-repair lane unchanged. |
| **Cursor** | Nothing pending on the video lane. |

## Guardrails unchanged

V2 frozen · V3 inactive until still stages pass · no paid Grok call · `temporalTrackingEnabled=false` until a still passes review · zero-deviation is the garment standard.
