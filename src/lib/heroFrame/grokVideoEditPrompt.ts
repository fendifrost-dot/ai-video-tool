/**
 * Frozen Grok /v1/videos/edits prompt for Architecture C (product lane).
 *
 * BLOCKED: awaiting Fendi confirmation of the exact string from
 * GROK_RECAP_FOUR_STEP_VERDICT.md §"Step 1 & 2" (external recap artifact).
 *
 * Do not ship billed runs with a paraphrased garment description. The prior
 * default described a navy track jacket with white stripes — that is not the
 * Saint Laurent Mastic Cotton Stand-Collar Jacket in the reference images.
 */
export const GROK_VIDEO_EDIT_PROMPT_READY = false as const;

/** Empty until Fendi confirms the frozen benchmark prompt string. */
export const GROK_VIDEO_EDIT_PROMPT = "";
