/**
 * Frozen Grok /v1/videos/edits prompt for Architecture C (product lane).
 *
 * BLOCKED: the Fendi-confirmed verbatim R4/R5 prompt string was requested for
 * install but is not present in this agent context (transcript / PR / repo /
 * Drive). External pointer: GROK_RECAP_FOUR_STEP_VERDICT.md §"Step 1 & 2".
 *
 * Do not paraphrase. Leave READY false until the exact string is pasted.
 * Prior wrong default (navy track jacket / white stripes) must not return.
 */
export const GROK_VIDEO_EDIT_PROMPT_READY = false as const;

/** Empty until Fendi pastes the confirmed frozen benchmark prompt string. */
export const GROK_VIDEO_EDIT_PROMPT = "";
